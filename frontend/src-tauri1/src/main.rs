// Десктоп-клиент учётной системы.
//
// Фронтенд ЗАШИТ в бинарник (dist), а API ходит по сети (см. tauri.conf.json →
// build.frontendDist). Поэтому окно открывается мгновенно и без интернета: офлайн-режим
// уже есть в самом фронте (IndexedDB + синхронизация), и он продолжает работать.
//
// Обёртка вокруг удалённого URL такого не дала бы: без сети не загрузилась бы даже
// оболочка.
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")] // без консольного окна

fn main() {
    tauri::Builder::default()
        .run(tauri::generate_context!())
        .expect("не удалось запустить приложение");
}
