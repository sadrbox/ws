/**
 * Администрирование 1С — ДВА РАЗДЕЛА, А НЕ ОДИН (21.09).
 *
 * Панель выросла из одного экрана с восемью вкладками, где рядом стояли базы кластера, сеансы, агенты и заявки.
 * Это разные предметы, и вопросы к ним разные: «что с базами этого сервера» и «какие службы у нас работают».
 * Поэтому раздел меню «Администрирование» состоит из двух:
 *
 *   «Кластеры 1С» (ClustersPane) — сервер 1С и всё, что внутри него: базы, сеансы, соединения, расширения,
 *      пользователи баз, обслуживание, прогресс операций. Кластер выбирается в тулбаре панели, рядом с состоянием
 *      его админ-агента: выбранный кластер и есть адресат всех запросов. Реестр самих кластеров — отдельный вид
 *      той же панели (кнопка «Реестр кластеров»), данные кластера идут во всю ширину.
 *   «Агенты» (AgentsPane) — службы, которые ходят в 1С: их роли (админ-агент со своим кластером и бизнес-агент
 *      со своей организацией), подключение по коду, активация БИН и процессы агентов.
 *   «Расширение БухПроф-AI» (ExtensionPane) — то, что стоит ВНУТРИ базы: заявки баз на подключение (приходят из
 *      формы 1С, а не от агента), парк баз с версиями расширения и состоянием чата, журнал вызовов по всем базам.
 *
 * ОБЩЕЕ ДЛЯ ОБОИХ. В кластер 1С ходит ТОЛЬКО агент и только по явной команде сервиса; браузер разговаривает с
 * `/v1/onec/*`. Данные делятся на три вида, и это определяет, что вызывает обращение к 1С:
 *   1. РЕЕСТР (базы, агенты, сводки) — таблицы БД сервиса: открытие вкладки и поиск в 1С не ходят;
 *   2. ЖИВОЕ СОСТОЯНИЕ (сеансы, соединения, процессы, лицензии) — всегда команда в кластер, кэша нет;
 *   3. СОДЕРЖИМОЕ БАЗЫ (пользователи, расширения) — вход в базу, минуты и занятый сеанс 1С: только по кнопке
 *      «Обновить», прочитанное оседает в реестре.
 * Любая команда заводит запись в реестре операций (progress.ts) — она видна в «Прогрессе» кластера.
 */
export { OneCClustersList } from "./ClustersPane";
export { OneCAgentsList } from "./AgentsPane";
export { OneCExtensionList } from "./ExtensionPane";

import { OneCClustersList } from "./ClustersPane";

/**
 * Прежний объединённый раздел. Оставлен ради панелей, восстановленных после перезагрузки (реестр представлений
 * помнит их по имени): открывает «Кластеры 1С». Новых ссылок на него в меню нет.
 */
export const OneCAdminList = OneCClustersList;

export default OneCClustersList;
