/**
 * Сколько длительной работы идёт прямо сейчас.
 *
 * ЗАЧЕМ ОТДЕЛЬНЫМ ФАЙЛОМ. Этим числом пользуется не список, а сама область: спиннер в её
 * шапке и на свёрнутой полосе. Держать хук в модуле-компоненте (ProgressSection) нельзя —
 * не-компонентный экспорт ломает Fast Refresh всему файлу.
 *
 * ЗАЧЕМ ВООБЩЕ. Счётчик сообщений говорит о случившемся, а проверка сотни баз идёт
 * минутами и не сообщает о себе ничего, пока не кончится: свернув область, человек
 * переставал знать, работает ли что-нибудь вообще.
 */
import { useOnecOps } from "src/models/OneCAdmin/progress";

export const useRunningOps = (): number =>
	useOnecOps().filter((o) => o.state === "running").length;
