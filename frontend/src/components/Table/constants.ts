/**
 * Константы виртуализации таблицы (T4 — вынесены из index.tsx, т.к. делят их
 * TableBody (расчёт видимого окна) и родительский Table (скролл активной строки
 * в центр). Отдельный модуль исключает циклический импорт между index и TableBody.
 */

/** Высота одной строки в px — базис виртуализации (позиция = index × ROW_HEIGHT). */
export const ROW_HEIGHT = 30;

/**
 * До скольких загруженных строк таблица рисуется ЦЕЛИКОМ, пока есть раскрытые строки.
 * Вложенные строки ломают допущение «все строки одной высоты», на котором стоит
 * виртуализация; на коротких списках проще отказаться от окна, чем подгонять отступы.
 */
export const EXPANDED_NO_VIRTUAL_LIMIT = 500;

/** Сколько строк дорисовывать сверху/снизу видимого окна (буфер против мельканий). */
export const OVERSCAN = 8;
