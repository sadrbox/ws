// ═══════════════════════════════════════════════════════════════════════════
// Токены ширины полей формы — единый источник вместо «магических» px.
//
// Раньше ширины задавались инлайн-литералами вразнобой (339px, 320px, 260px,
// 150px, 180px…). Здесь — канонический набор по СМЫСЛУ поля. Используйте токен
// вместо px, чтобы поля одного назначения были одинаковой ширины во всех формах.
// ═══════════════════════════════════════════════════════════════════════════

export const FIELD_WIDTH = {
  /** Номер документа (короткое цифро-буквенное поле). */
  number: "150px",
  /** Дата / дата-время. */
  date: "180px",
  /** Денежная сумма, селект среднего размера. */
  amount: "200px",
  /** Узкое поле (код, ставка, количество). */
  sm: "120px",
  /** Среднее поле. */
  md: "200px",
  /** Стандартное широкое поле формы (реквизиты-лукапы). */
  lg: "340px",
  /** Очень широкое поле (поиск сотрудника/пользователя). */
  xl: "400px",
} as const;

export type FieldWidthToken = keyof typeof FIELD_WIDTH;
