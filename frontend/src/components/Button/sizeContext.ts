/**
 * РАЗМЕР КНОПОК ПО УМОЛЧАНИЮ ДЛЯ ОБЛАСТИ ЭКРАНА (19.09).
 *
 * Тулбар пейна (PaneItemToolbar) собирают разные компоненты: FormPanel («Сохранить», «Закрыть»), кнопки форм через
 * `afterClose` («Операции» карточки базы), тулбары списков. Проставлять `size="sm"` в каждом значило бы получить
 * тулбар, где одна забытая кнопка крупнее соседей. Область задаёт размер одним провайдером; явный `size` у кнопки
 * по-прежнему главнее. Кнопки-иконки (IconButton) этим не управляются — у них свой размер.
 *
 * Отдельным модулем: не-компонентный экспорт в модуле с компонентом ломает Fast Refresh.
 */
import { createContext } from "react";

export type ButtonSize = "sm" | "md" | "lg" | "min";

export const ButtonSizeContext = createContext<ButtonSize | undefined>(undefined);
