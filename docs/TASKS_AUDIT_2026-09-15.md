# Аудит согласованности: агент 1С ↔ сервис ↔ панель — задачи от 15.09.2026 (01:30)

Сверка по коду пяти направлений: коды отказов и подсказки; блокировка сеансов, эхо изменяющих команд,
реестр баз, `AGENT_HEALTH`; каталог команд, поля, сроки, идемпотентность; задания и долгие операции
(проверка базы, процессы, отмена); статусы в документах задач. Код агента — сборка `2026-09-15 01:06`
(на сервере `00:32`), сервис `W:\app\ai\src`, панель `W:\app\frontend\src` — по состоянию файлов на 01:30.

Предыдущий аудит — `TASKS_AUDIT_2026-09-14.md` (А1–А13, С1–С16, П1–П12). **Все его пункты со статусом
«сделано» сверены с кодом — подтверждены; открыт С16.**

**Правило: после каждого исправления отметить пункт здесь — сборкой агента или коммитом.**

## Статус

> **Номера письма 16.09 и документа.** В письме задачи названы А39–А41; здесь эти номера заняты (А39 — объяснение обрыва связи, А40 — журнал «Система», А41 — собеседник по порту). Соответствие: письмо А39 = **А43**, письмо А40 = **А44**, письмо А41 = **А45**.

| Часть | Сделано | Осталось |
|---|---|---|
| Агент | А14–А48 — сборки `15:13`–`2026-09-16 23:45`; А50, А51 — `2026-09-17 19:16` ([дубль пользователя](TASKS_ONEC_USER_DUPLICATE_2026-09-17.md)); **на сервере `19:16` с 17.09** — самопроверка `_transition` 20 из 20, в том числе «справочник пользователей читается» и «роль назначена при создании» (А42) | А49 (низкий); скрытая база — задачи сервиса и панели, агенту нет ([разбор](TASKS_ONEC_HIDDEN_BASE_2026-09-17.md)) |
| Сервис | С16–С35 — сделаны (сверено по коду 16.09) | **С36** — подсказка к `IB_CONNECTION_LOST` не должна обещать того, чего в тексте агента нет |
| Панель | П13–П22 — сделаны (сверено по коду 16.09) | одиночные долгие операции после истечения срока — проверить на сервере (П16) |
| Сервер 1С | — | К1 (падение `rphost` на проверке `_transition`) |

> **Сверка 15.09 после `840e269` (сервис и панель; по коду).** Сделаны С16, С19–С23, П15, П16 — подтверждены
> С16 (`LIMITS_NOT_SET`), С20 (`dispatchedAt`), С22 (`dryRun` вне очереди базы), следы С19/С21/С23 (`attempt`,
> `late`, `agent.cancel.check`). Запрошенное у агента — А16 (план до замка базы), А20 (`commandId` в строках
> процессов), А21 (снятие `1cv8` осмотра и способность `agent.cancel.check`) — **уже в сборке `2026-09-15 15:31`**
> (на сервере `00:32`). **Высокие, не сделанные по коду:** С17 — итог `IB_CHECK` в задании (`issues`/`repaired`
> в `ai/src/onec` не читаются); С18 — место базы после `TIMEOUT` не держится (с агентом `15:31` команда поверх
> конфигуратора не ляжет — агент держит базу и ответит `IB_BUSY`, — но задание получает отказ и повторы впустую);
> П13 — `warning` загрузки и обновления (в `IbRestoreResult`/`IbApplyUpdateResult` поля нет, `api.ts:564,571`;
> итог — зелёный тост, `BaseMaintenance.tsx:185`); К1 — падение `rphost`. **Не сделаны и важны для только что
> сделанного:** С30 — `commandId` отбрасывается схемой процессов, и П15 процесс долгой операции не покажет; С26 —
> блокировка из ответа загрузки и обновления не применяется (`writeState.ts:101-113`). П14 — одиночная проверка
> при `issues > 0` по-прежнему зелёным тостом (`BaseMaintenance.tsx:165-166,185`).
> **Сверка после `faca33a` (15.09).** Сервис и панель: С17, С18, П13, П14 — сделаны; С26, С30 — частично. Агент:
> все задачи аудита сделаны — последняя сборка `2026-09-15 16:23` (А24, А25, А27–А29), на сервере `00:32`.
> Высокие открытые — только К1 (падение `rphost`, сервер 1С).
> **Сверка после `b806507` (15.09).** Сервис и панель: С24–С31, П17–П21 — сделаны. Агент `2026-09-15 16:48`: А30
> (ожидание исполнителя по `expiresAt`), А31 (`sessionId` — только UUID). Открыто: сервису — доделать С26 и С30
> (процесс долгой операции во вкладке «Процессы»), С32; сервер 1С — К1; установка агента `16:48` (на сервере `00:32`).
> **Сверка по коду 15.09 17:00 (сервис и панель против агента).** Сделаны С16–С23, С26–С32, П13–П15, П17–П20;
> частично С24, С25, П16 (остаток С20 — признак «агент на связи»); П19 — сделан иначе; **П21 — регрессия**.
> Расхождения на стыке закрыты в агенте `2026-09-15 17:30` (А32–А35); панели — П21, П22. Актуальный список
> открытого — `E:\Development\bpapi_agent\docs\ROADMAP.md`, раздел «Открытые задачи».
> **Решение по С24 — В2 (15.09 18:00).** Срок команды продлевается, пока агент подтверждает работу: heartbeat
> сообщает выполняемые команды (`running`), сервис продлевает их срок и объявляет это при регистрации
> (`runningLease`). Агентская часть — А36, сборка `2026-09-15 18:00`; сервисная — С33.
**Порядок.** Сначала то, что оставляет базу закрытой или кладёт работу поверх работающего конфигуратора:
А14 + С18 + П13. Затем потерянный итог проверки: С17 + П14. Затем С16. Остальное — по мере работы.

## Главное

1. **Снятие долгой команды (предел агента или отмена) бросает работу посреди.** Блокировка входа у
   загрузки и обновления не снимается; конфигуратор продолжает работать, но пропадает из «Процессов
   агента»; агент и сервис освобождают место базы — следующая команда ложится поверх. Текст «снята
   агентом» и подсказка «Агент прервал команду» это скрывают (А14, С18).
2. **Итог проверки базы в задании теряется**: проверка нашла ошибки — в «Заданиях» и «Прогрессе»
   «Выполнено» (С17, П14).
3. **«База закрыта для входа» после загрузки или обновления панель не показывает** (П13).
4. **Долгая операция непрозрачна**: «0 из 1 · Выполняется» одинаково для очереди, работы и зависшего
   конфигуратора; проверку без «Исправлять» не прервать (С20, С23, П15, А20, А21).

## Агент

| № | П | Задача | Статус |
|---|---|---|---|
| А14 | В | **Снятие долгой команды бросает работу посреди.** Предел (`agent.rs:1018`, `tokio::time::timeout`) и отмена (`cancel.rs`) отбрасывают задачу: (1) снятие блокировки в `with_sessions_locked` стоит после работы в той же задаче (`commands/mod.rs:1466-1472`) и не выполняется — у `IB_RESTORE`/`IB_APPLY_UPDATE` база остаётся закрытой, возможно с окном «сейчас — сутки» (`:1507-1509`); (2) `1cv8` запущен без `kill_on_drop` и живёт, а запись о процессе снимается вместе с `Ticket` (`procs.rs:164-168`) — в «Процессах агента» его нет, снять нельзя (`procs.rs:258`); (3) замок базы агента отпускается (`commands/mod.rs:929`) — следующая команда по базе стартует поверх живого конфигуратора; (4) текст «снята агентом … увеличьте предел» неверен. Сделать: снятие блокировки — при любом завершении (охранник, в т.ч. при обрыве); живой процесс снятой команды — в «сироты» с командой и базой; замок базы держать, пока процессы команды живы; в тексте `TIMEOUT` — какие процессы продолжают работу (pid) и снята ли блокировка. `ibcmd` при обрыве убивается посреди загрузки (`ib/ibcmd.rs:368`) — в тексте назвать, что база могла остаться недогруженной | **сделано `2026-09-15 15:13`**: живой `1cv8` снятой команды остаётся в процессах (без команды, с `commandId`), замок базы держится до его завершения, блокировку входа снимает охранник после завершения процессов, текст `TIMEOUT` называет живые процессы (`procs::live_detached`, `UntilDetachedExit`, `UnlockWhenDetachedExit`, `commands::timeout_message`; тест) |
| А15 | С | **`IB_CONNECT_FAILED` не переклассифицируется.** Мост отдаёт его на любой отказ подключения (`bridge.ps1:265`), а разбор переводит в `IB_BUSY`/`IB_CONNECTION_LOST` только `IB_ERROR` (`ib/mod.rs:1260,1264`). «Монопольный режим» или обрыв 10054 при подключении приходят кодом, которого сервис не знает: не повторяются по С10, без состояния кластера. Сделать: те же правила для `IB_CONNECT_FAILED`; код — в контракт (Д1) | **сделано `2026-09-15 15:13`** (`parse_bridge_answer`; тест) |
| А16 | С | **Сухой прогон ждёт замок базы.** Замок берётся до проверки `dryRun` (`commands/mod.rs:929` раньше `:944`): пока идёт долгая операция, план не получить — через 60 с `IB_BUSY`. Сделать: план — до замка. Пара к С22 | **сделано `2026-09-15 15:13`** (план — до замка базы) |
| А17 | Н | **Загрузка и обновление не отдают блокировку после снятия.** Агент перечитывает её в кэш (`commands/mod.rs:1486`), а в ответ `state.lock` не кладёт (`echo_after_write`, `:1128-1131`) — панель до полного среза (до 300 с) показывает «Вход закрыт». Сделать: `state.lock` в ответе. Пара к С26, П13 | **сделано `2026-09-15 15:13`** (`with_sessions_locked`: `state.lock`, прочитанный после снятия) |
| А18 | Н | **`cluster.locks.lastRefusal.at` — секунды Unix** (`cluster/mod.rs:971`), панель ждёт строку и показывает январь 1970 (`agentHealth.ts:111`). Сделать: RFC3339; единицы — в контракт | **сделано `2026-09-15 15:13`** (`unix_rfc3339`; тест) |
| А19 | Н | **Повторное `CLUSTER_TERMINATE_SESSION` — ошибка.** «Сеанс не найден» от `rac` возвращается отказом (`cluster/mod.rs:1975-1980`), контракт обещает SUCCESS на повтор (контракт :193): повтор после 202 даёт 422. Сделать: «не найден» — `ok` с признаком «уже снят» и эхом | **сделано `2026-09-15 15:13`**: `{ok: true, sessionId, alreadyGone: true}`, только если «не найден» ответили все кластеры (`session_gone`; тест) |
| А20 | Н | **Строки `AGENT_LIST_PROCESSES` без номера команды.** Запись знает команду (`procs.rs:152`), наружу поле не уходит (`procs.rs:39-60`). Сделать: `commandId` в строке — чтобы панель связала долгую операцию с её процессом. Пара к С20, П15 | **сделано `2026-09-15 15:13`** (`commandId` в строке; сервис его пока отбрасывает — С30) |
| А21 | Н | **Проверку без «Исправлять» не прервать и отменой.** Отмена `1cv8` не снимает никогда (`procs.rs:235`), а осмотр `-TestOnly` базу не меняет. Сделать: снимать `1cv8` отменённой `IB_CHECK` без `repair`; в ответе отмены — что снято. Пара к С23, П15 | **сделано `2026-09-15 15:13`** (`procs::mark_disposable`: отмена без `force` и `TIMEOUT` снимают конфигуратор осмотра); **способность `agent.cancel.check` объявляется с `15:31`** — без неё сервис прерывание проверки не разрешал (`admin.ts:755`) |
| А22 | Н | **Мелочи `IB_CHECK`.** Отчёт обрезается до 600 символов без пометки (`ib/mod.rs:1291-1297`); `logicalIntegrity: false` без `repair` молча не действует (`ib/mod.rs:621-629`) — решение на стороне панели (П20), агенту — пометка обрезки | не требуется: отчёт уже помечен «…» (`ib::short`), флажок — П20 |
| Д1 | Н | **Контракт `ONEC_AGENT_CLUSTER_CONTRACT.md`.** Дописать: таблицу всех кодов отказов с поведением сервиса (повтор, отметка «в базу не войти») — нет `IB_TIMEOUT`, `IB_UNAVAILABLE`, `IB_ERROR`, `IB_DB_MISSING`, `IB_CONNECT_FAILED`, `RAS_UNREACHABLE`, `IB_FORBIDDEN`, `IB_ROLES_UNREADABLE`, `INFOBASE_USER_NOT_FOUND`, `IB_BAD_PAYLOAD`, `IB_PUBLISH_FAILED`, `AGENT_STOPPED`, `AGENT_STOPPING`, `AGENT_PROCESS_UNSAFE`, `AGENT_PROCESS_NOT_FOUND`, `UNKNOWN_COMMAND`; ответ `CLUSTER_DISCONNECT` (сейчас `{ok:true}` против таблицы эха); что сервис вырезает из результата (только `users`/`extensions`); `IB_CHECK` — `repairMode`, пример `keys` с `/IBCheckAndRepair`; формы `AGENT_CANCEL_COMMAND` и строк `AGENT_LIST_PROCESSES`; пределы агента 600/14 400 с и момент отсчёта (от получения пропуска); срок бизнес-команд (контракт :37 — 900, сервис — 600); пример способностей и выдачу с `baseKey`; `lastRefusal.at`; `reason`/`byQuery` у `CLUSTER_CHECK_BASES` | **сделано 15.09 15:10** — контракт 623 → 1166 строк: выдача и `requestId`, регистрация, способности и таблица «команда → способность», heartbeat и прощание, payload `CLUSTER_DROP_INFOBASE`/`CLUSTER_CHECK_BASES`/`IB_RESTORE`/`IB_APPLY_UPDATE`/`IB_CHECK`, процессы и отмена, все коды агента и сервиса (§4), противоречие `instanceId` снято; пункты 5–10 сессии сервиса |
| А23 | С | **Опрос команд встаёт при заполненном ожидании.** Место ожидания берётся до опроса (`agent.rs:331-335`), мест столько же, сколько пропусков (`max_parallel`, по умолчанию 4). Если столько команд уже ждут пропуска (пачка кластерных команд поверх четырёх долгих операций), опрос не открывается: через 10 с сервис считает агента «не на связи» (`ai/src/agents/service.ts:19`, `POLL_GAP_MS`), а команды управления агентом (отмена, процессы), которым пропуск не нужен, не доходят. Сделать: опрос не закрывать ради команд `AGENT_*`; запас мест ожидания больше числа пропусков | **сделано `2026-09-15 16:05`**: место ожидания перед опросом ждётся не дольше 5 с, затем опрос открывается без него — команды управления агентом доходят, агент «на связи»; взятая команда ждёт исполнителя не дольше разницы сроков сервиса и агента (обычная 300 с, долгая 600 с) и иначе НЕ выполняется — ответ `AGENT_BUSY` (`executor_wait_cap`, `refuse_unstarted`; тест). Сервису — С31 |
| А24 | Н | **Проверка баз данных открывает базу мимо замка.** `CLUSTER_CHECK_BASES` и фоновая `db_probe_loop` запускают `ibcmd extension list` (чтение, до 5 с) по базам, которые не решил запрос к СУБД, без замка базы (`cluster/mod.rs:1461-1506`): во время загрузки, проверки или обновления той же базы операция может получить «монопольный доступ». Сделать: базы с идущей операцией агента пропускать с причиной «идёт операция» | **сделано `2026-09-15 16:23`**: базы с занятым замком (`commands::base_busy`) проверка через `ibcmd` пропускает с причиной «по базе идёт операция агента» и на сутки не откладывает; фон — так же (`probe_db`); тест |
| А25 | Н | **Отказ heartbeat пишется только на уровне debug** (`agent.rs:1285`). Сервис отвечает 400 при нарушении пределов полей (`what` > 200, `tool` > 50, строк процессов > 200, баз > 500 — `agentRouter.ts`): процессы и базы перестают обновляться молча, а «на связи» остаётся. Сделать: предупреждение в журнале и в `AGENT_HEALTH` | **сделано `2026-09-15 16:23`**: HTTP 4xx на heartbeat — предупреждение в журнале (при первом отказе и смене текста) и `agent.heartbeatRejected {at, message}` в `AGENT_HEALTH`, снимается при приёме; тест |
| А26 | В | **Ложное прощание после потери связи.** `on_cloud_error` ставит состояние `Offline`, а heartbeat отправлял состояние как есть; пока шли команды, оно не возвращалось в `Online`, и первый heartbeat после восстановления связи уходил со `status: "OFFLINE"` — сервис читает это как прощание (`agents/instances.ts:62`) и снимает владение с живого процесса (контракт, аудит 15.09). | **сделано `2026-09-15 15:31`**: `OFFLINE` уходит только прощанием при остановке, иначе дошедший heartbeat — `ONLINE` (`state::wire_status`, `FAREWELL`; тест) |
| А27 | Н | **`AGENT_HEALTH` может показать `ib.admin`, которого агент не объявлял**: сводка считает способности по заданному служебному администратору без пробы входа (`health.rs:52`), регистрация — по пробе (`agent.rs:834`). Сделать: то же условие, что при регистрации | **сделано `2026-09-15 16:23`**: способности в сводке — по подтверждённому входу, как при регистрации (`Runtime::ib_confirmed`); в сводке и `agent.ibConfirmed` |
| А28 | Н | **`CLUSTER_DROP_INFOBASE` отказывает одним кодом**: «база работает», «нет пароля СУБД» и прочие защитные отказы приходят как `RAC_ERROR` — по коду не отличить от сбоя `rac`. Сделать: свои коды защитных отказов, в контракт | **сделано `2026-09-15 16:23`**: `DB_PASSWORD_MISSING`, `IBCMD_UNAVAILABLE`, `DB_PARAMS_UNAVAILABLE`, `INFOBASE_DB_ALIVE`, `DB_CHECK_FAILED` (`ClusterError::Refused`; тест). Сервису — С32 |
| А29 | Н | **`pollMaxWaitSecs` и `basesFullEverySecs` из ответа сервиса агент не читает** (`cloud/mod.rs:94-101`, `protocol.rs:164-169`): ожидание опроса и частота полного среза — только из настроек агента. Сделать: учитывать или убрать из контракта | **сделано `2026-09-15 16:23`**: ответ регистрации разбирается, опрос ждёт не дольше `pollMaxWaitSecs` (`poll_wait`; тест); `basesFullEverySecs` — в журнал, полный срез по-прежнему по `wantFullBases` |
| А30 | Н | **Ожидание исполнителя — по сроку из выдачи.** Сервис с `b806507` передаёт `expiresAt` (С31), а агент ждал исполнителя по разнице сроков из контракта (А23). | **сделано `2026-09-15 16:48`**: ждёт до `expiresAt` за вычетом предела работы агента (ответ приходит до истечения срока; остатка нет — только свободный исполнитель, иначе `AGENT_BUSY`, сервис повторит с паузой; предел 0 — до самого срока); без `expiresAt` — как в А23; срок, истёкший уже при получении, — одно предупреждение о расхождении часов (`executor_wait`; тест) |
| А31 | Н | **`sessionId` в `CLUSTER_TERMINATE_SESSION` — только UUID** (контракт; сервис с `b806507`, С27): агент принимал любую строку и передавал её `rac`. | **сделано `2026-09-15 16:48`**: не UUID (8-4-4-4-12) — `VALIDATION_ERROR`, `rac` не вызывается (`is_uuid`; тест) |
| А32 | С | **Сухой прогон долгой команды сразу получал `AGENT_BUSY`** (сверка 17:00, Б2): предел брался по типу (14 400 с) при сроке сервиса 900 с — ожидание исполнителя обнулялось, план загрузки или обновления за занятыми исполнителями не получить. | **сделано `2026-09-15 17:30`**: сухой прогон — с обычным пределом (`runs_long`; тест) |
| А33 | Н | **Ответ впритык к сроку** (Б3): ожидание до `expiresAt − предел`, предел отсчитывается от пропуска — ответ мог уйти ровно в момент срока, после закрытия команды сервисом. | **сделано `2026-09-15 17:30`**: запас 60 с на сеть и часы (`ЗАПАС_ДО_СРОКА`; без `expiresAt` — 240/540 с; тесты) |
| А34 | Н | **Отложенная база в `CLUSTER_CHECK_BASES` узнавалась по тексту** (Б5). | **сделано `2026-09-15 17:30`**: у такой строки `busy: true`. Панели — П22 |
| А35 | Н | **Повтор снятия процесса с `force` не предлагался** (Б1): отказ `AGENT_PROCESS_NOT_FOUND` содержал «уже завершился», и панель считала процесс снятым. | **сделано `2026-09-15 17:30`**: слова убраны из отказа без `force` (после `force` — «на сервере нет — он уже завершился», как и должно). Панели — П21 |
| А36 | Н | **Агентская часть В2 — продление срока выполняемых команд (решение по С24, 15.09).** Срок сервиса задан заранее, а длительность загрузки и обновления большой базы неизвестна: при пределе агента больше срока работающая команда закрывалась как просроченная. | **сделано `2026-09-15 18:00`**: heartbeat и `AGENT_HEALTH` — `running: [{commandId, type, startedAt}]` (только начатые, пустой список — тоже ответ); способность `agent.running`; при `runningLease: true` в ответе регистрации ожидание исполнителя оставляет до срока запас только до первого heartbeat, а не весь предел работы (`cancel::running`, `executor_wait`; тесты) |
| А37 | С | **Отказ установки расширения — неверный текст и двойной вызов** (сервер, 15.09, агент `18:00`, `_transition`): «платформа не знает метод Записать / Write: … Ошибка применения расширения конфигурации: Значение контролируемого свойства Тип у объекта Справочник.Номенклатура.Реквизит.Артикул не совпадает со значением в расширяемой конфигурации». Мост перебирал имена метода при ЛЮБОЙ ошибке: метод, который нашёлся и отказал, вызывался вторым именем (COM 1С понимает оба), а наверх уходило «не знает метод». | **сделано `2026-09-15 23:02`**: перебор имён — только при незнакомом имени (`DISP_E_UNKNOWNNAME`, «Method not found»), иначе сразу текст платформы (`Invoke-Any`, `Test-UnknownMember`, `Get-PlatformText`; проверено на COM-объекте: отказ существующего метода — без второго вызова); «ошибка применения расширения» — код `IB_EXTENSION_NOT_APPLICABLE` с советом, на COM-пути и у `ibcmd` (`extension_not_applicable`, `extension_refusal`; тест). Сервису — С34; расширению — К2 **Подтверждено на сервере 16.09**: отказ пришёл кодом `IB_EXTENSION_NOT_APPLICABLE` с текстом платформы и советом, без «не знает метод». |
| А38 | Н | **Версия конфигурации базы — по запросу** (15.09): агент отдавал её только в эхе загрузки и обновления — у большинства баз в карточке «—»; при отказе «расширение не применимо» (К2) версия базы была не видна. Фоновое чтение — не нужно (решение 15.09). | **сделано `2026-09-15 23:24`**: команда `IB_INFO` — только чтение: конфигурация (имя, версия, синоним) и расширения одним входом через COM, расширения — с откатом на `ibcmd`, регистрация и блокировка — из `rac`; блоки `state.*` в форме эха, `config` — и отдельным полем (`base_info`; тест). Установка расширения (COM): `config` в ответе, при отказе записи — «[конфигурация базы: имя версия]» в тексте **Подтверждено на сервере 16.09**: в отказе — «[конфигурация базы: БухгалтерияДляКазахстана 3.0.69.1]». |
| А39 | С | **Объяснение обрыва пропадало целиком** (сервер, 16.09, пятый прогон проверки `_transition`): отказ чтения списка процессов уносил всю подсказку — ни перезапущенных процессов, ни пределов кластера, ни записей журнала «Приложение»; у отказа остался один текст платформы, а подсказка сервиса уверяла, что агент всё перечислил. | **сделано `2026-09-16 08:22`**: подсказка строится из прочитанного; непрочитанный список процессов называется прямо, а не «перезапуска не видно»; вывод о падении делается и по записи журнала Windows; пустая подсказка не отправляется, в журнал — предупреждение (`connection_lost_hint`, `explain_connection_lost`; тест). Сервису — С36 |
| А40 | С | **Перезапуск служб 1С не виден** (сервер, 16.09, диагностика агента `08:22`): в журнале «Приложение» за 7 суток — «сбоев нет», а обрыв 10054 приходит и от `ragent` (`localhost:1540`) на обычных вызовах `rac` — значит процесс гасит не исключение, а перезапуск службы, и запись об этом лежит в журнале «Система», куда агент не смотрел. | **сделано `2026-09-16 08:52`**: `crashes.rs` вторым запросом читает журнал «Система» (`Service Control Manager` за тот же срок, отбор по тексту `1C:Enterprise\|1С:Предприятие\|1cv8\|BPAPIAgent`), строки получают поле `log`; разбираются события 7031/7034 («завершилась непредвиденно», с числом попыток), 7024 (код выхода), 7000/7009 (не запустилась), 7036 (работает/остановлена). Попадают и в текст `IB_CONNECTION_LOST`, и в «Сбои процессов 1С за 7 суток»; вывод о падении делается и по событию службы (тест). Запрос проверен на живом журнале |
| А41 | С | **Собеседника всегда называли «рабочим процессом»** (сервер, 16.09): обрыв от `localhost:1540` — это агент сервера (`ragent`), от `1541` — менеджер кластера (`rmngr`), от `1545` — сервер администрирования (RAS); текст отказа всё равно писал «связь с рабочим процессом кластера 1С оборвалась» и уводил разбор не туда. | **сделано `2026-09-16 08:52`**: `ib::lost_peer(low)` определяет собеседника по порту из текста платформы (1540 — агент сервера, 1541 — менеджер кластера, 1545 — сервер администрирования, иначе рабочий процесс) и подставляется в отказы загрузки, обновления и работы конфигуратора (тест) |
| А42 | В | **Новый пользователь заводился без прав** (жалоба 16.09): `IB_CREATE_USER` с ролями отвечал успехом, а роли у пользователя не появлялись. Роли выдавались объекту, которого в базе ещё НЕТ: «СоздатьПользователя» отдаёт несохранённого пользователя, и его коллекция ролей через внешнее соединение живёт сама по себе — «Очистить» и «Добавить» проходят без единой ошибки, а запись сохраняет пользователя БЕЗ ролей. Правка ролей (`IB_UPDATE_USER`) работала всегда — там пользователь уже записан в базе. Самопроверка не ловила: роли она сверяла только после правки. | **сделано `2026-09-16 09:23`**: мост сперва записывает пользователя, перечитывает его по имени и назначает роли на записанном объекте — тем же путём, что и правка; после записи роли сверяются с базой, расхождение — отказ `IB_FIELD_NOT_APPLIED` («создан, но платформа не сохранила роли: …»), назначенные роли — в ответе полем `roles`; `IB_CREATE_USER` принимает и `addRoles` (панель правит права поправками); роль, пришедшая не строкой, — отказ `IB_BAD_PAYLOAD`, а не молчаливый пропуск; в самопроверку добавлен шаг «роль назначена при создании». **На сервере проверить:** создать пользователя с ролями и убедиться, что права на месте |
| А43 | В | **«База данных заблокирована» уходит как поломка** (сервер, 16.09, агент `09:23`, `_transition`): установка расширения отказала текстом платформы «Ошибка разделенного доступа к базе данных. База данных заблокирована: компьютер: SERVER, сеанс: 2, начат: 16.09.2026 в 9:54:27, приложение: Фоновое задание». Агент переводит в `IB_BUSY` только «монопольн»/«exclusive» (`ib/mod.rs:1283`), поэтому отказ ушёл кодом `IB_ERROR`: групповое задание его НЕ повторяет (сервис повторяет единственный код — `IB_BUSY`), а панель показала «Не выполнено», как поломку агента. Сделать: «разделенного доступа», «база данных заблокирована», «database is locked», «shared access» → `IB_BUSY` с сохранением текста платформы | **сделано `2026-09-16 12:13`**: «разделенного доступа», «база данных заблокирована», «database is locked», «shared access» переводятся в `IB_BUSY` (`ib::db_locked`) — и у моста, и на путях конфигуратора (выгрузка, загрузка, проверка/обновление), где текст платформы теперь сохраняется в отказе, а не заменяется своим; тест |
| А44 | В | **Не видно, кто держит базу**: в отказе назван один сеанс из текста платформы, а сколько их всего и что это за сеансы — неизвестно. Сделать: к отказу по занятости прикладывать снимок сеансов базы из кластера (`rac session list` по базе): сеанс, приложение («Фоновое задание», «Конфигуратор», клиент), компьютер, пользователь, начало; фоновые и регламентные задания называть отдельно — чаще всего базу держат именно они | **сделано `2026-09-16 12:13`**: `ib::lock_holder` разбирает текст платформы в поля `error.details.lockedBy` — `computer`, `session`, `application`, `user?`, `startedAt` (ISO) и `startedText`; метка исполнителя в `details` сохраняется (`protocol::executor_details`); тест. Снимок сеансов из кластера в отказ не кладётся: держатель уже назван полями, а лишний вызов `rac` в момент отказа затягивал бы ответ |
| А45 | Н | **Регламентные задания мешают установке расширения**: расширение, меняющее структуру данных, применяется монопольно, а собственные фоновые задания базы этот режим ломают. Сделать необязательный признак команды (`denyScheduledJobs`): агент через `rac` ставит запрет регламентных заданий базы, выполняет операцию и ВОЗВРАЩАЕТ прежнее значение, а в ответе называет, что менял и вернул. По умолчанию выключено — признак меняет состояние базы | **сделано `2026-09-16 12:13`** отдельной командой `CLUSTER_SET_SCHEDULED_JOBS` `{baseKey, denied}` (`rac infobase update --scheduled-jobs-deny`): в ответе `denied`, прежнее значение `was` и эхо `state.lock` со `scheduledJobsDenied`. Возврат прежнего значения делает тот, кто ставил команду — агент сам ничего не возвращает; тест на объявление и на аргументы `rac`. Сервису и панели — С39 и П26 |
| А46 | Н | **Не видно, на что уходит время**: установка расширения шла 3 мин 53 с до отказа, удаление — 4 мин 3 с, и из ответа не понять, где время: вход в базу (типовая на БСП при внешнем соединении выполняет обновление ИБ), чтение списка расширений, запись, применение. Сделать: раскладку по этапам в ответе и в отказе, и её же в журнал агента | **сделано `2026-09-16 12:37`**: мост меряет этапы сам (`Start-Stages`/`Add-Stage`) — вход в базу, чтение файла расширения, поиск расширения в базе, запись и применение, удаление, эхо состояния; отказ посреди работы даёт этап «до отказа», иначе самые долгие минуты в раскладку не попадали. Успех несёт `data.stages` (`[{name, ms}]`) и пишет их в журнал агента, отказ — строку «. Время по этапам: вход в базу 3 мин 12 с; …», которую дописывает агент ПОСЛЕ обрезки текста платформы (`ib::stages_text`); тест. Механизм проверен живым запуском моста: `{"code":"INFOBASE_NOT_FOUND","stages":[{"name":"до отказа","ms":7879}]}` |
| А47 | В | **Запрет регламентных заданий отвечал запросом, а не фактом** (аудит эха 16.09): `denied` в ответе `CLUSTER_SET_SCHEDULED_JOBS` был повтором payload, а сервис берёт его РАНЬШЕ `state.lock.scheduledJobsDenied` (`writeState.ts:126`). Кластер, принявший `rac infobase update` и не изменивший признак, записывал в реестр «запрещены». | **сделано `2026-09-16 23:45`**: `denied` — прочитанный после записи; расходится с запросом — `warning`; не прочиталось — поля нет, `unverified: ["denied"]`; запрошенное — в `requested` (`commands::scheduled_jobs_echo`); тест |
| А48 | Н | **Блокировка сеансов: `enabled` — тоже повтор запроса**, а кластер, не изменивший блокировку, получал предупреждение про окно, которое ни при чём. | **сделано `2026-09-16 23:45`**: `enabled` — факт из эха, `requested` — запрос; не изменилась — `warning` «после записи по-прежнему …»; не прочиталась — `unverified: ["enabled"]` (`commands::sessions_lock_fact`); тест |
| А49 | Н | **Публикация без эха молчит**: `IB_PUBLISH` без `urlConfirmed` и любая публикация на Apache отвечают без `state.publication` и без пометки `unverified` — по ответу не отличить «проверено» от «не проверялось». У Apache судить нельзя сознательно (снятие не удаляет `default.vrd`). Сделать: `unverified: ["publication"]`, когда эха нет | открыт |
| А52 | С | **Конфликт блокировок приходит общим кодом.** Живой случай 17.09 на `_transition` (агент `2026-09-16 23:45`): `IB_UPDATE_USER` отказал через 20 с текстом платформы «Конфликт блокировок при выполнении транзакции: Превышено максимальное время ожидания предоставления блокировки» с кодом `IB_ERROR` — это ТАЙМАУТ УПРАВЛЯЕМОЙ БЛОКИРОВКИ, то есть занятость ДАННЫХ, а не базы: транзакция откатилась, ничего не записано, и та же правка минутой позже прошла с первого раза. Сервис узнаёт такой отказ по тексту (`0875e65` и раньше `5388640`) и повторяет команду задания — но текст зависит от языка платформы. Сделать: отвечать `IB_BUSY` (текст платформы сохранять) на «Конфликт блокировок», «Превышено максимальное время ожидания предоставления блокировки», «взаимоблокировк», англоязычные «Lock conflict», «lock request timeout», «deadlocked»; полями — `error.details.lockConflict: {kind: "timeout"|"deadlock", waitedMs?}`, а если платформа назвала держателя — существующий `details.lockedBy`. Тогда повтор в задании и кнопки панели (П25) перестанут зависеть от разбора текста | открыт |

## Сервис

| № | П | Задача | Статус |
|---|---|---|---|
| С16 | С | **Подсказка к `IB_CONNECTION_LOST`** советует «поднимите пределы» без условия (`onec/errorHints.ts:125-129`), хотя агент пишет «пределы перезапуска и памяти не заданы» (все три варианта вывода). Сделать: при этой фразе — «процесс упал: журнал «Приложение», дампы `rphost`, проверка на копии базы»; искать фразу с «не заданы» (в самой подсказке есть «пределы перезапуска и памяти кластера»); тест. Подробности — `TASKS_ONEC_AUDIT_2026-09-14.md` | **сделано 15.09 `840e269`** (при «пределы перезапуска и памяти не заданы» — «процесс упал: журнал, дампы»; тест) |
| С17 | В | **Итог `IB_CHECK` в задании теряется.** В строку элемента идёт только `path`/`url` (`onec/batches.ts:166`), предупреждение собирается лишь для пользователей (`:168-170`). Сделать: итог из `issues`/`repaired`/`skipped`, при `issues > 0` — `warning`. Пара к П14 | **сделано 15.09 `faca33a`** (`checkOutcome`: итог «найдено / исправлено / не выполнено без «Исправлять»» в строке задания, при оставшихся ошибках — `warning`; тест) |
| С18 | В | **`TIMEOUT` освобождает место базы.** Место держит только `COMMAND_EXPIRED` (`commands/queue.ts:88-91`), а после `TIMEOUT` агента конфигуратор может работать (А14): следующая команда или «Повторить неуспешные» ложится поверх. Подсказка «Агент прервал команду» (`errorHints.ts:121`) неверна. Сделать: `TIMEOUT` у долгих команд внутрь базы держит место, как истёкшая; подсказка — «агент перестал ждать; процесс может продолжать работу, база может быть закрыта для входа — см. текст» | **сделано 15.09 `faca33a`** (`TIMEOUT_STILL_RUNNING`: место базы держится, пока в снимке процессов есть процесс с `commandId` команды, и 90 с после; «Повторить неуспешные» такую не трогает; `stillRunning` в строке задания; подсказка «агент перестал ждать») |
| С19 | С | **Повтор `IB_BUSY` без паузы.** Копия ставится сразу (`queue.ts:546-566`), проверка сеансов отвечает за секунды — три попытки сгорают мгновенно, С10 не помогает. Номер попытки в строку задания не попадает (`batches.ts:165-174`). Сделать: откладывать повтор на N минут; номер попытки в элементе. Пара к П14 | **сделано 15.09 `840e269`** (пауза 2/5 мин — `available_at`, миграция 027; `attempt`/`retryAt` в строке задания) |
| С20 | С | **Долгая операция не видна.** `GET /commands/:id` одинаково отвечает для `queued` и `dispatched` (`http/onecRouter.ts:1020-1034`); `startedAt` агента не сохраняется (`queue.ts:516-524`); для выданной команды без агента текст «поставлена в очередь, но забрать её некому» (`onecRouter.ts:1027-1031`) неверен. Сделать: `state`, `dispatchedAt`, «агент на связи» в ответе «ещё идёт»; поправить текст. Пара к П15, А20 | **сделано** (сверено 16.09: `agentOnline` и `agentSilentSecs` в ответе «ещё идёт», `onecRouter.ts:1084-1111`) |
| С21 | С | **Поздний результат после истечения** принимается молча (`queue.ts:513-525`): в карточке и журнале навсегда «Не выполнено»; «Повторить неуспешные» до прихода результата ставит дубль (`batches.ts:297`, у задания нет `requestId`). Сделать: отметка «поздний результат»; не повторять выданную истёкшую, пока она держит место. Пара к П16 | **сделано 15.09 `840e269`** (`late`; повтор не трогает истёкшую, пока она держит место; `late`/`lateWait` в строке задания) |
| С22 | С | **Сухой прогон — команда внутрь базы** (`onecRouter.ts:216`): ждёт места за долгой операцией (при `AGENT_IB_PARALLEL=1` — одно), `priority:-1` не помогает; контракт обещает «впереди» (:343-345). Сделать: `inBase: false` для `dryRun`. Пара к А16 | **сделано 15.09 `840e269`** (`inBase: false` у `dryRun`); на стороне агента — А16 |
| С23 | С | **`IB_CHECK` без `repair` нельзя прервать**: класс WRITE (`commands/admin.ts:446`), прерывается только чтение (`admin.ts:750-752`, `onecRouter.ts:1103-1106`). Сделать: разрешить прерывание осмотра (после А21) | **сделано 15.09 `840e269` с условием**: прерывание проверки без «Исправлять» — только агенту со способностью `agent.cancel.check`; агент объявляет её со сборки `2026-09-15 15:31` (А21) |
| С24 | Н | **Сроки не согласованы с агентом.** Срок сервиса — от выдачи, предел агента — от получения пропуска (`agent.rs:406-426`), запас 300/600 с съедает очередь агента (`max_parallel`); бизнес-команды чата — 600 с при пределе агента 600 (`chat/workflow.ts:303`). Сделать: срок = предел агента (`AGENT_HEALTH`: `commandTimeoutSecs`/`longCommandTimeoutSecs`) + запас; бизнес-командам — больше 600 | **закрыт решением В2** (15.09): срок продлевается, пока агент подтверждает работу — С33 сделан |
| С25 | Н | **Подсказки.** `SESSIONS_LOCK_NOT_ACTIVE` — «исправление в агенте ожидается, аудит А7» устарело (`errorHints.ts:133`; А7 на сервере с 00:05); нет подсказки и правила повтора для `IB_TIMEOUT` и `AGENT_STOPPING` (агент пишет «можно повторить»); `IB_AUTH_FAILED` определяется по тексту (`errorHints.ts:73,106,144`) — проверять код; `IB_SELFTEST` без служебного администратора — «нет в сборке» (`agents/features.ts:48`), а на деле не задан служебный администратор базы | **сделано** (сверено 16.09: `IB_TIMEOUT` в `RETRY_LATER_CODES`, `queue.ts:155-156`; подсказка `errorHints.ts:148`) |
| С26 | Н | **Эхо.** `state.lock` у `IB_RESTORE`/`IB_APPLY_UPDATE` применять (`onec/writeState.ts:101-113`; после А17); `CLUSTER_DROP_INFOBASE` со `stillListed: true` — всё равно `markMissing` (`writeState.ts:93-97`); хранить `permissionCodeSet` (отбрасывается в `parseLock`, `writeState.ts:40-50`) | **сделано 15.09** (`faca33a` — `state.lock` у загрузки и обновления; `b806507` — `stillListed` у `CLUSTER_DROP_INFOBASE` → `markMissing`, `permissionCodeSet` хранится, миграция 028) |
| С27 | Н | **`CLUSTER_TERMINATE_SESSION`**: `sessionId` — проверка формата UUID в схеме (`admin.ts:158`, сейчас строка 1..64) | **сделано 15.09 `b806507`** (`sessionId` — UUID) |
| С28 | Н | **Комментарии в коде устарели**: `admin.ts:452-454` (без `repair` агент только осматривает, переиндексации нет); `admin.ts:579` (`CLUSTER_CHECK_BASES` отдаёт `reason`, `byQuery`); склейка повторов захватывает `IB_BACKUP` (`admin.ts:697`, `onecRouter.ts:235`) | **сделано 15.09 `b806507`** (комментарии `IB_CHECK`, `CLUSTER_CHECK_BASES` с `reason`/`byQuery`, склейка выгрузки — намеренно) |
| С29 | Н | **Эксплуатация.** Перезапуск после миграций 025, 026, S1–S3, R1–R5, С1–С15, П1–П12 («действует после `pm2 restart all`») не отмечен ни в одном документе — выполнить и отметить в `CHECKS_SERVER_2026-09-13.md`, пункт 0. `AGENT_LATEST_BUILD` в `ai/.env` = `2026-09-15 00:12` — после установки агента `01:06` | **частично 15.09**: `AGENT_LATEST_BUILD` = `2026-09-15 23:24` (16.09); **осталось** — `pm2 restart all` (миграции 025–028) и отметка в `CHECKS_SERVER_2026-09-13.md`, пункт 0 (запускает пользователь) |
| С30 | Н | **`commandId` в строках процессов** (агент `15:13`, А20) отбрасывается: схема `processes` в heartbeat (`http/agentRouter.ts:95-102`), `parseProcesses` (`onec/writeState.ts:53-67`), тип `AgentProcess` в панели. Нужен для П15 — связать долгую операцию с её процессом. Там же: `alreadyGone` у `CLUSTER_TERMINATE_SESSION` — успех; `state.lock` у `IB_RESTORE`/`IB_APPLY_UPDATE` агент отдаёт с `15:13` — С26 можно делать | **сделано 15.09** (`faca33a` — `commandId` у процессов; `b806507` — `alreadyGone`: панель показывает «Сеанс уже был снят») |
| С31 | Н | **Новый код агента `AGENT_BUSY`** (сборка `16:05`, А23): команда ждала свободного исполнителя дольше разницы сроков (обычная 300 с, долгая 600 с) и НЕ выполнялась. Сделать: подсказка «агент был занят другими командами — команда не выполнялась, повторите»; в задании повторять, как `IB_BUSY` (С19, с паузой); отметку «в базу не войти» не ставить. По желанию — срок команды (`expiresAt`) в выдаче: агент мог бы ждать ровно до него, а не по разнице сроков из контракта | **сделано 15.09 `b806507`** (подсказка к `AGENT_BUSY`; повтор в задании с паузой, как `IB_BUSY`; «в базу не войти» не ставится; `expiresAt` в выдаче команды) |
| С32 | Н | **Новое в агенте `16:23`.** Коды защитных отказов `CLUSTER_DROP_INFOBASE` вместо `RAC_ERROR` (А28): `DB_PASSWORD_MISSING`, `IBCMD_UNAVAILABLE`, `DB_PARAMS_UNAVAILABLE`, `INFOBASE_DB_ALIVE` («база работает» — удалять нельзя), `DB_CHECK_FAILED` — подсказки; `markMissing` по ним не ставить. `AGENT_HEALTH`: `agent.heartbeatRejected {at, message}` (сервис не принимает heartbeat — процессы и базы не обновляются) и `agent.ibConfirmed` — показать в «Состоянии сервера» (А25, А27). `CLUSTER_CHECK_BASES`: причина «не проверялась: по базе идёт операция агента» (А24) — не ошибка | **сделано** (сверено 16.09: подсказки к кодам удаления регистрации; отложенные базы — не ошибка, `checkBasesDb.ts:36`) |
| С33 | С | **Сервисная часть В2 — продление срока выполняемых команд (решение по С24, 15.09).** Принять в heartbeat `running: [{commandId, type, startedAt}]` (агент `18:00`, способность `agent.running`; до 100 строк, `commandId` ≤ 64). Для каждой перечисленной ВЫДАННОЙ команды этого агента продлевать `expires_at` до max(текущий, приём heartbeat + 3 × период heartbeat, не меньше 90 с), но не дальше потолка от выдачи (например, 24 ч). Не перечислена или heartbeat не пришёл — не продлевать: зависший или остановленный агент выявляется истечением срока, как сейчас. В ответе регистрации — `runningLease: true` (агент по нему оставляет до срока запас только до первого heartbeat). С18 (`TIMEOUT_STILL_RUNNING`) не трогать. По желанию — в панели «выполняется, агент подтверждает с …» | **сделано** (сверено 16.09: `runningLease: true`, разбор `running`, продление срока — `agentRouter.ts:115-118,323-362`) |
| С34 | Н | **Новый код агента `IB_EXTENSION_NOT_APPLICABLE`** (сборка `23:02`, А37): платформа не применила расширение к конфигурации базы (контролируемое свойство заимствованного объекта не совпало и т. п.). Подсказка: «расширение собрано под другую версию конфигурации — пересоберите его (обновите заимствованные объекты или снимите контроль свойства)»; в задании не повторять; отметку «в базу не войти» не ставить. Текст агента уже содержит платформенную причину и совет | **сделано** (сверено 16.09: `errorHints.ts:168`; отметка «в базу не войти» не ставится — `bases/service.ts:232`); подтверждено ответом сервера 16.09 |
| С35 | Н | **`IB_INFO` — сведения о базе** (агент `23:24`, А38). Сервис: спецификация (только чтение, способность `ib.admin`, команда внутрь базы — с местом базы, срок обычный, `payload {baseKey}` + `auth` при выдаче); применять ответ тем же разбором эха — `state.config` (`setConfig` с `readAt`), `state.extensions`, `state.lock`; `IB_INSTALL_EXTENSION` — `config` в ответе применять так же. Панель: кнопка «Обновить сведения» в карточке базы; версия конфигурации — со временем чтения. Фоновое чтение не нужно (решение 15.09). **Уточнено при сверке 16.09:** `config` у `IB_INSTALL_EXTENSION` — поле ответа, не `state`; `setConfig` писал через `COALESCE` — `version: null` («не задана») оставлял прежнюю версию; `state.extensions` уже применял общий `parseEcho`; путь `/bases/:key/info` занят `CLUSTER_INFOBASE_INFO`; нужен признак сборки — на сервере агент `18:00` без `IB_INFO` | **сделано** (сверено 16.09: `admin.ts:397`, `onecRouter.ts:440`, `writeState.ts:144`; кнопка в карточке по способности `IB_INFO`) |
| С36 | Н | **Эхо ложится в реестр позже, чем команда стала `done`** (сверка С35, 16.09). `queue.complete` будит ожидание (`agentRouter.ts:540`), а `planWriteState` и `parseEcho` применяются после (`:608`, `:692`): синхронный ответ панели и опрос `/commands/:id` могут вернуться раньше записи, и перечитанный реестр покажет прежнее. Противоречит правилу в том же обработчике («значение обязано быть там уже к этому моменту»). Сделать: применять эхо и `planWriteState` до `queue.complete` (по `pending`), состояние от агента правдиво при любом приговоре | **сделано 16.09 `9190783`**: до `queue.complete` применяются срез баз `CLUSTER_LIST_INFOBASES`, проверка баз данных, `planWriteState`, публикация и её срез, эхо содержимого, отметка «в базу не войти» и кэш `IB_LIST_*`; запись команды читается при любом исходе (отметку ставит и отказ). После завершения остались повтор занятой базы, отметка позднего результата, закрытие прерванной, чтения «чего эхо не принесло», журнал и аудит. Состояние применяется и тогда, когда результат потом не приняли (поздний по отменённой), — оно правдиво. Обходной путь в ручке «Обновить сведения» убран |
| С36 | Н | **Подсказка к `IB_CONNECTION_LOST` обещает больше, чем есть** (сервер, 16.09): «Агент перечислил процессы… и пределы перезапуска и памяти кластера» — а в тексте агента их может не быть, если опросить кластер не удалось (со сборки `2026-09-16 08:22` агент пишет «список рабочих процессов прочитать не удалось»). Сделать: говорить о процессах и пределах только когда они есть в тексте агента (признак — «Что видно в кластере»); иначе — «агент не смог опросить кластер: смотрите журнал агента и «Отладочную информацию»» | **сделано** (сверено по коду 17.09): подсказка стала условной — «Если агент привёл выше процессы… Если их выше нет, прочитать кластер агенту не удалось» (`errorHints.ts:209-214`); осталась мелочь — С46. (Номер С36 в документе занят дважды — выше пункт сессии сервиса про `queue.complete`) |
| С38 | Н | **Одиночная команда панели после `IB_BUSY` не повторяется**: повтор есть только у команды группового задания (`retryBusy`, `queue.ts`). Человек, нажавший «Установить расширение» на одной базе, получает «Не выполнено» и должен сам догадаться нажать снова — а с А43 таких отказов станет больше. Сделать: повторять и одиночную теми же паузами (120/300 с) либо отдавать панели признак «можно повторить» с готовой кнопкой | **сделано 16.09 `1cbd289`** (отказ отдаётся панели с признаком `retryable` и полями держателя; кнопка «Повторить» в сообщениях — отдельной задачей: модель сообщения не знает действий) |
| С39 | Н | **Новая команда `CLUSTER_SET_SCHEDULED_JOBS`** (агент `2026-09-16 12:13`): `{baseKey, denied}`, ответ `{ok, baseKey, denied, was, state.lock}`. Принять в очередь как кластерную CRITICAL-команду и провести в панель; в отказе `IB_BUSY` пробросить `error.details.lockedBy` (поля держателя базы) | **сделано 16.09 `9fb2152`** (команда, маршрут `/bases/:key/scheduled-jobs`, признак в реестре, миграция 030) |
| С40 | В | **Запрет заданий: запрос записывается как факт** (`writeState.ts:122-130`, `service.ts:752`): признак берётся из `result.denied` раньше `state.lock.scheduledJobsDenied`, а при их отсутствии — из payload, и во всех случаях `scheduled_jobs_seen_at = now()`: панель показывает «запрещены · 23:45», как будто прочитано у кластера. Сделать: порядок `state.lock.scheduledJobsDenied` → `denied` (с агента `23:45` — факт) → payload; у значения из payload — отметка источника «команда» и без времени чтения (как `sessions_denied_source`) | **сделано** (сверено по коду 17.09): порядок «эхо блокировки → `denied` ответа → payload» и источник `scheduled_jobs_source`; значение по команде пишется без времени чтения (`writeState.ts:122-139`, `bases/service.ts:771-781`) |
| С41 | Н | **Оговорки одиночных команд не доходят до итога операции**: `warning` и `unverified` у `CLUSTER_SET_SCHEDULED_JOBS` и `CLUSTER_SET_SESSIONS_LOCK`, `requestedName` и `skipped` у `IB_INSTALL_EXTENSION` — сервис разбирает оговорки только у записи пользователя (`userWriteWarning`, `writeBack.ts:59`). Сделать общий разбор оговорок по типу команды — для пакета (`warning` строки) и для одиночной | **сделано** (сверено по коду 17.09): общий разбор оговорок `commandCaveat` (`onec/caveats.ts`) — `warning`, `unverified`, `requestedName`, `skipped`; доходит и до одиночной команды (`onecRouter.ts:336-338`), и до строки задания (`batches.ts:271`) |
| С42 | Н | **Держатель базы: сервис затирает поля агента своим разбором** (`onecRouter.ts:315-319`): `parseLockedBy` разбирает текст, уже обрезанный агентом до 400 символов (приложение в хвосте может пропасть), и кладёт `{computer, sessionId, startedAt, appId}` поверх агентского `details.lockedBy` `{computer, session, application, user?, startedAt (ISO), startedText}`. Сделать: брать поля агента и приводить к форме панели (`session`→`sessionId`, `application`→`appId`, `startedText`→`startedAt`, ISO — отдельным полем); свой разбор — только для агентов старше `2026-09-16 12:13` | **сделано** (сверено по коду 17.09): `normalizeLockedBy(details.lockedBy)` от агента, свой разбор текста — только запасной (`onecRouter.ts:323-325`) |
| С46 | Н | **Подсказка к `IB_CONNECTION_LOST` отстала от агента** (сверка 17.09): текст всегда говорит «связь с рабочим процессом кластера 1С» и зовёт в журнал «Приложение», а агент с `2026-09-16 08:52` называет собеседника по порту (`ragent`, `rmngr`, RAS или рабочий процесс, А41) и сам читает журнал «Система» (А40). Сделать: не навязывать «рабочий процесс» и назвать оба журнала | **сделано 17.09 `0875e65`:** подсказка говорит «оборвалась связь со службой 1С», собеседника называет агент по порту (1540 ragent, 1541 rmngr, 1545 RAS), в совете — оба журнала Windows: «Система» (перезапуски служб) и «Приложение» (падения rphost) |

## Панель

| № | П | Задача | Статус |
|---|---|---|---|
| П13 | В | **«Снять блокировку не удалось — база закрыта для входа» не показывается.** Агент кладёт `warning` в ответ `IB_RESTORE`/`IB_APPLY_UPDATE` (`commands/mod.rs:1472-1478`), в типах панели поля нет (`services/onec/api.ts:542,549`), итог — зелёный тост (`OneCBases/BaseMaintenance.tsx:149,153`). Сделать: поле в типах, предупреждение в итоге и журнале | **сделано 15.09 `faca33a`** (`warning` в типах; итог загрузки и обновления — предупреждение в тосте и в журнале операции) |
| П14 | С | **Итог проверки базы.** Одиночная: тост зелёный и при `issues > 0`, в журнале «Выполнено» без чисел (`BaseMaintenance.tsx:89-92,155`), `keys` не показаны. «Задания»: в строке элемента «Итог» — «—» (`OneCAdmin/BatchesTab.tsx:271`), `warning` (П12) там тоже не виден. Сделать: предупреждение с числами, итог и `warning` в строке задания (после С17), номер попытки (после С19) | **сделано 15.09 `faca33a`** (одиночная проверка — предупреждение с числами, ключи в отчёте; строка задания — итог, `warning`, попытка, повтор, «процесс ещё работает», поздний результат) |
| П15 | С | **Долгая операция в карточке.** Показывать «ждёт очереди / выполняется с …», процесс `1cv8` с pid и переход к «Процессам агента» (после С20, А20); «Прервать» у проверки без «Исправлять» (после С23, А21); ответ отмены — `note`/`killed` (сейчас не показываются, `force` не шлётся — `BatchesTab.tsx:204`, `QueueHolders.tsx:30`); подсказка «запись, выгрузку и обновление не обрывают» для осмотра неверна (`BatchesTab.tsx:114-117`) | **сделано 15.09 `840e269` частично**: «ждёт очереди / выполняется с …» и «Прервать» в «Обслуживании», `note`/`killed` в ответе прерывания, подсказка у проверки; **осталось** — процесс `1cv8` с pid и переход к «Процессам агента» (после А20) |
| П16 | С | **Поздний результат.** Операция закрывается один раз и опрос прекращается (`OneCAdmin/progress.ts:210-231,268`; `BatchesTab.tsx:151-154`) — досматривать истёкшие выданные ~600 с и сообщать о смене итога (после С21) | **сделано у заданий** (`lateWatch`, `progress.ts:215-226`); одиночные долгие операции — проверить на сервере |
| П17 | Н | **Блокировка и эхо.** «Закрыт, но не действует» утверждает «осталось окно прошлой блокировки» (`i18/translations.json:1736`), а окно могло быть задано на будущее, и значение до 30 мин старое — нейтрально «вне окна с … по …» и время чтения (`OneCAdmin/sessionsLock.ts`); `stillListed` у разрыва соединения (`ConnectionsTab.tsx:72-73`) и у удаления регистрации (`OneCBases/BaseAvailability.tsx:73`) — предупреждение; код разрешения задан (после С26) | **сделано 15.09 `b806507`** («Закрыт, но не действует» — «текущее время вне окна» и время чтения; «код разрешения задан»; `stillListed` у разрыва соединения и удаления регистрации — предупреждение) |
| П18 | Н | **Ожидание обычных команд** — 15 мин (`api.ts:106,112`), а сервис даёт до 900 с очереди + 900 с выполнения: «выполняется слишком долго» при живой команде | **сделано 15.09 `b806507`** (ожидание обычных команд 30 мин) |
| П19 | Н | **`fullName`.** Карточка шлёт `""` и очищает имя при подсказке «оставить как есть» (`OneCAdmin/userUpdate.ts:106,115`; `BaseUserForm.tsx:632`); групповые пути очистить имя не могут (`userUpdate.ts:88,93`, `BaseUserWizard.tsx:275`, `GroupCommandWizard.tsx:175`) | **сделано иначе** (`b806507`): очистить полное имя нельзя нигде (решение 15.09) — сохранение с пустым заблокировано |
| П20 | Н | **«Логическая целостность» без «Исправлять»** ни на что не влияет (`BaseMaintenance.tsx:50-53,215`): осмотр всегда проверяет целостность — делать недоступной без «Исправлять», как переиндексацию (П9) | **сделано 15.09 `b806507`** (без «Исправлять» — включена и недоступна, в команду не уходит) |
| П21 | Н | **Повтор с `force` после `AGENT_PROCESS_NOT_FOUND`.** С `force` агент снимает процесс вне своего списка только если образ — `rac`/`ibcmd`/`powershell`/`webinst`; иначе отвечает тем же кодом `AGENT_PROCESS_NOT_FOUND` (другим текстом: «уже завершился» или «не инструмент агента»). Панель (`OneCAdmin/ProcessesTab.tsx:101-107`) не должна предлагать `force` повторно, если он уже был | **сделано** (сверено 16.09: «уже завершился» проверяется только после `force`, `ProcessesTab.tsx:113`) |
| П22 | Н | **`CLUSTER_CHECK_BASES`: отложенная база — по признаку.** Агент `17:30` отдаёт у строки `busy: true` (А34); панель узнаёт её по тексту причины (`checkBasesDb.ts:40`) — правка формулировки молча превратила бы отложенные базы в «не проверены» | **сделано** (сверено 16.09: признак `busy`, текст — запасной путь, `checkBasesDb.ts:40-41`) |
| П23 | Н | **Сведения о базе в карточке** (С35). Кнопка «Обновить сведения» — чтение, доступна и просмотру; сборка без `IB_INFO` — недоступна с «Нет в этой сборке: сведения о базе»; конфигурация — со временем чтения, три состояния: не читали, «версия не задана», версия | **сделано 16.09 `d8b20ff`** (`fetchBaseInfo`; `configLabel` — «имя версия · прочитано …»; кнопка в «Реквизитах», операция в «Прогрессе»; признак `info` в «Нет в этой сборке»; RU/KK). Визуально не выверено |
| П24 | Н | **Задание, упёршееся в занятую базу, показано как «Выполнено»** (16.09, установка расширения на `_transition`): «1 из 1 · 100% · Выполнено, длительность 20 мин 28 с», а ниже — «база занята, повтор 16.09.2026 09:30». Команда не выполнена ни разу: все попытки получили `IB_BUSY`, и назначен новый повтор. Зелёное «Выполнено» на невыполненной работе читается как успех, а 20 минут выглядят как длительность установки, хотя это ожидание очереди. Сделать: такому заданию — свой вид («отложено: база занята, повтор в ЧЧ:ММ»), а в длительности разделять ожидание очереди и работу агента | **не дефект** (проверено по базе сервиса 16.09: задание `f9b514d9` выполнилось с третьей попытки, две первые — `IB_TIMEOUT`; вводило в заблуждение устаревшее примечание про повтор — исправлено в `6b00fde`) |
| П25 | Н | **Отказ по занятой базе без разбора**: показан один текст платформы. Сделать: выводить сеансы, держащие базу (агент приложит их по А44), выделять фоновые задания и «Конфигуратор», рядом — кнопки «Повторить» и «Показать сеансы базы» | **частично 16.09 `1cbd289`** (сервис разбирает держателя в `details.lockedBy`, панель показывает его строкой в тексте отказа; кнопки «Повторить» и «Показать сеансы» ждут расширения модели сообщений) **Сверено по коду 17.09:** `retryable` доходит до панели (`services/ai/endpoint.ts:88-99`), кнопок «Повторить» и «Показать сеансы» в компонентах нет — остаётся открытым **Сделано 17.09 `e40ce0a`:** `useOnecErrorActions` строит обе кнопки (повтор — только когда сервис назвал отказ повторимым); «Показать сеансы» открывает карточку базы на вкладке «Сеансы» со списком и подсветкой строки по номеру сеанса — снимают сеанс там же, по строке, потому что команде кластера нужен UUID; подключено в обслуживании, публикации и командах пользователя базы |
| П26 | Н | **Держатель базы и запрет заданий в карточке**: показать `lockedBy` (компьютер, сеанс, приложение, начало) из отказа `IB_BUSY`, рядом — «Снять сеанс» и «Запретить регламентные задания» (`CLUSTER_SET_SCHEDULED_JOBS`), а после операции предложить вернуть прежнее значение по `was` | **сделано 16.09 `9fb2152`** (строка «Регламентные задания» и кнопка запрета в карточке базы) |
| П27 | С | **«Запретить / Разрешить регламентные задания» не показывает итог по факту** (`OneCBases/index.tsx:430-434`): `onSuccess` только перечитывает реестр, `warning` и `unverified` из ответа не видны — кнопка отработала «успешно», а задания по-прежнему разрешены. Сделать: итог операции из ответа (факт `denied`, `warning`, «не проверено»); подпись времени — только у прочитанного у кластера (после С40); после работ — «Вернуть как было» по `was`, а не переключатель, не знающий исходного состояния | открыт |
| П28 | Н | **Время по этапам не показывается** (агент `12:37`, А46): у успеха `data.stages` нигде не выводится. Сделать: в итоге долгой внутрибазовой операции (установка и удаление расширения, пользователи) — строка «вход в базу 3 мин 12 с; запись 41 с»; у отказа раскладка уже в тексте | открыт |
| П29 | Н | **Установка расширения: другое имя и пропущенные свойства не видны**: `requestedName` (расширение встало под именем из `.cfe`, а панель ищет запрошенное — «расширения нет в списке») и `skipped` не выводятся. Сделать: предупреждение в итоге операции (после С41 — и в пакете) | открыт |
| П30 | Н | **Блокировка сеансов: «не проверено» не отличается от «применено»** (`SessionsTab.tsx:185`): учтены `warning` и `echo.active`, а `unverified: ["enabled"]` (агент `23:45`) — нет. Сделать: при `unverified` — оговорка «кластер не отдал состояние, проверьте в консоли» | открыт |

## Сервер 1С

| № | П | Задача | Статус |
|---|---|---|---|
| К1 | В | **`IB_CHECK` на `_transition` роняет `rphost`** (15.09 00:23; 00:41 и 00:42 — два процесса за один прогон; все пределы кластера и рабочего сервера 0). Не дефект А1: ключи приняты, падает процесс платформы. **На рабочем сервере не повторять** — каждое падение рвёт сеансы других баз. Разбор: агент `01:06` → «Отладочная информация» → «Сбои процессов 1С за 7 суток» (модуль, код исключения); дампы `rphost`; проверка на копии базы; при ошибке платформы — обновление релиза или обращение в поддержку 1С. Владелец — администратор сервера 1С. **Повторилось 16.09** (агент `23:24`): `IB_CHECK` на `_transition`, 1 мин 53 с, `tcp://SERVER:1561 … 10054`; процессов и пределов кластера агент к тексту НЕ приложил (контракт обещает их — см. `ONEC_AGENT_CLUSTER_CONTRACT.md:1074`), значит прочитать кластер ему не удалось — разбор только по отчёту окна агента, журналу «Приложение» и дампам. Подсказка сервиса переписана на условную (`6c33530`+) | открыт **16.09 — пятый прогон** (агент `23:24`): снова обрыв (`tcp://SERVER:1561`, 10054) через 1 мин 53 с; объяснение агента не построилось — А39. Записи о падениях `rphost` — в «Отладочной информации», раздел «Сбои процессов 1С за 7 суток». **16.09 08:22 — диагностика агента:** в журнале «Приложение» за 7 суток — «сбоев нет», то есть `rphost` не падает с исключением; при этом 10054 приходит и от `ragent` (`localhost:1540`) на обычных вызовах `rac` («не удалось получить список баз кластера»), а самопроверка `_transition` прошла 18 из 18. Значит рвётся не один рабочий процесс, а соединения с разными службами 1С. Отсюда А40 и А41. Дальше смотреть: журнал «Система» (7031/7034 по службам 1С — со сборки `2026-09-16 08:52` агент читает его сам и показывает в «Сбоях процессов 1С»), технологический журнал платформы (`EXCP`, дампы) и сторонние сетевые фильтры (антивирус, firewall), способные рвать локальные TCP-соединения. |
| К2 | С | **Расширение для `_transition` не применимо к её конфигурации** (15.09, `IB_INSTALL_EXTENSION`): контролируемое свойство «Тип» у `Справочник.Номенклатура.Реквизит.Артикул` в расширении не совпадает с конфигурацией базы. Не дефект агента и не базы: расширение собрано под другую версию конфигурации. Открыть расширение в конфигураторе на конфигурации этой базы, обновить заимствованные объекты (или снять контроль свойства «Тип» у реквизита), сохранить `.cfe`, установить заново. Владелец — разработчик расширения | открыт **16.09: версия конфигурации базы известна из отказа — `БухгалтерияДляКазахстана 3.0.69.1`**; пересобирать расширение под неё. Агент и сервис своё сделали. |

## Ответы сессии сервиса — вопросы 11–17 (15.09 15:13, по коду агента)

| № | Вопрос | Ответ |
|---|---|---|
| 11 | Терпит ли агент `auth` в кластерных командах с `baseKey` | **Да.** `payload` разбирается без строгой схемы (`cloud/protocol.rs:178-188`), кластерные команды читают только свои поля, `auth` не используют (`rac` — учётная запись из настроек). Payload не журналируется (только id, тип, код; из `auth` — имя пользователя, не пароль), в ответ и spool не попадает. Оговорка: пароль базы уходит в командную строку `1cv8 /P` и `ibcmd --password` — виден администраторам сервера в диспетчере задач |
| 12 | `force` при `AGENT_PROCESS_NOT_FOUND` | **Частично.** С `force` снимается процесс вне списка агента, если его образ — `rac`, `ibcmd`, `powershell` или `webinst` (`procs.rs` `terminate_unknown`). Иначе снова `AGENT_PROCESS_NOT_FOUND` («уже завершился» / «не инструмент агента»): повтор с `force` не предлагать (П21). `1cv8` вне списка, `rphost`, `rmngr`, `ragent` не снимаются никогда; `1cv8` из списка — с `force` |
| 13 | Идёт ли `CLUSTER_CHECK_BASES` параллельно командам внутрь баз | **Да, обходит.** Замка базы не берёт, место в очереди сервиса не занимает (`inBase=false`), у агента — один пропуск на всю проверку. Делает: `rac infobase info`, один запрос списка баз к СУБД (чтение), для нерешённых — `ibcmd extension list` (чтение, до 5 с). Риск отказа «монопольный доступ» у идущей операции той же базы — А24 |
| 14 | Переоткрывает ли агент опрос во время долгой работы | **Да**: команды уходят в отдельные задачи, опрос идёт дальше, heartbeat — отдельно раз в 30 с. **Но** если `max_parallel` команд уже ждут пропуска, опрос встаёт — А23 |
| 15 | Обновляет ли агент кэш блокировок после своей `CLUSTER_SET_SESSIONS_LOCK` | **Да**: кэш сбрасывается и заполняется прочитанным после изменения; полный срез отдаёт его при следующем `wantFullBases`. Не прочиталось — `lock` в срезе нет до фонового чтения (после отказа — пауза 6 ч). С `15:13` блокировка перечитывается и после загрузки и обновления (и отдаётся в ответе, А17) |
| 16 | `instanceId` — заголовок или тело | Агент шлёт заголовок `X-Agent-Instance` на каждом запросе и поле `instanceId` в теле регистрации и heartbeat; сервис владение проверяет по заголовку |
| 17 | Сломает ли `commandId` в строке процесса heartbeat | **Нет**: схемы — обычный `z.object`, лишние ключи отбрасываются. До панели поле не доходит — С30 |
## Проверено и согласовано

* **Каталог.** 33 спецификации сервиса (с С35 — 34, `IB_INFO`) = админ-типы агента без `IB_BATCH` (не объявляется, в спецификациях и заданиях его нет); способности `cluster.admin`, `ib.admin`, `ib.roles`, `agent.procs`, `agent.cancel`, `ib.auth` совпадают; набор долгих команд одинаков (`IB_BACKUP`, `IB_RESTORE`, `IB_CHECK`, `IB_APPLY_UPDATE`).
* **Поля** `IB_CREATE_USER`/`IB_UPDATE_USER` (`showInList`, `addRoles`/`removeRoles`, `roles: []`), `IB_RESTORE`/`IB_APPLY_UPDATE` (`lockSessions`, `backup`), `IB_CHECK` (`repair` и ключи), `IB_BACKUP`, `IB_PUBLISH`, `IB_INSTALL_EXTENSION`, `AGENT_KILL_PROCESS`, `AGENT_CANCEL_COMMAND`, `CLUSTER_DISCONNECT`, `CLUSTER_DROP_INFOBASE` (`confirm`), `AGENT_LOG_TAIL`, учётная запись `auth` при выдаче — совпадают.
* **Блокировка.** Форма `state.lock` ↔ `parseLock`; `reset`/`note`/`warning` у `CLUSTER_SET_SESSIONS_LOCK` доходят до панели (П10); путь `lock` из полного среза в реестр (А6) и колонка `sessions_denied_active` (миграция 026); `SESSIONS_LOCK_NOT_ACTIVE` — код, подсказка, контракт.
* **Эхо.** `users`/`extensions` только полными списками, `IB_LIST_*` удаляют отсутствующее; `CLUSTER_TERMINATE_SESSION` — UUID из панели, эхо `sessions` с `complete`/`stillListed`; `CLUSTER_DISCONNECT` — `connections` и `locks` в кэши панели; `config`, `processes`, `publication` — формы совпадают.
* **`AGENT_HEALTH.cluster.locks`** — форма совпадает с П11 (кроме типа `at` — А18); **П12** — путь `unverified`/`skipped` от моста до итога операции (кроме строки «Заданий» — П14).
* **Отказы.** С10: `IB_BUSY` повторяется до 3 раз, `IB_CONNECTION_LOST` — нет; отметки «в базу не войти» по кодам (`IB_AUTH_FAILED`, `IB_DB_MISSING`, `INFOBASE_NOT_FOUND`), не для сухого прогона; `IB_FIELD_NOT_APPLIED` → перечитывание пользователей; `AGENT_PROCESS_UNSAFE`/`AGENT_PROCESS_NOT_FOUND` — согласие в панели; все 9 кодов раздела 4 контракта агент отдаёт.
* **Идемпотентность и ожидание.** `requestId` генерирует сервис, склейка — среди стоящих и выданных; панель очередь не снимает (только неначатую, кнопкой); сервис ждёт синхронно 20 с, затем 202 — как в контракте («45 с» в коде нет).
* **R1–R5, А1–А13, С1–С15, П1–П12** — статусы «сделано» подтверждены кодом.

## Исправлено в документах при аудите (15.09 01:30)

* `TASKS_ONEC_AUDIT_2026-09-14.md`: сборка на сервере; П11, П12 — сделаны; колонка «Статус» у С15–С16; «Для сессии агента» 1–3 — закрыты; С7 — сделан.
* `TASKS_AUDIT_2026-09-14.md`: таблица соответствия T1–T8 (T1, T5, T7 — сделаны, T8 — можно ставить); падение `rphost` — К1, а не А1; ссылка на этот документ.
* `CHECKS_SERVER_2026-09-13.md`: дата состояния; развёртывание сервиса — не подтверждено (С29); ожидаемая сборка в разделе 1 — `01:06`; «Итоги» 1 и 5.
* `TASK_AGENT_ECHO_WRITE_COMMANDS.md`: абзацы «Сервису» и «окно блокировки» — закрыты (S1–S5, А7).
* `TASKS_DEV_2026-09-14.md`: задачи R7 — все сделаны, таблица R7 — состояние до исправлений.
* Копии в `E:\Development\bpapi_agent\docs` сведены с `W:\app\docs`: плашки «ВЫПОЛНЕНО СЕРВИСОМ» перенесены в `TASK_SERVICE_DROP_INFOBASE.md`, `TASK_SERVICE_IB_MAINTENANCE.md`, `TASK_SERVICE_PROCESSES.md`; `TASKS_DEV_2026-09-14.md` и `TASK_SERVICE_ECHO_WRITE_COMMANDS.md` — из `W:\app\docs`.
