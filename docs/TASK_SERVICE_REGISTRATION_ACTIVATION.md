# Задача сервису и панели: регистрация баз, активация БИНов, много агентов (СВ4, ПН)

19.09.2026. Контракт — [CONTRACT_BASE_REGISTRATION_2026-09-19.md](CONTRACT_BASE_REGISTRATION_2026-09-19.md).
Сторона 1С (расширение `buhprof_api` 1.5.0) и агент (сборка 19.09, окно с вкладкой «Базы и БИНы») готовы и
проверены на заглушках. Ниже — что сделать в `W:\app\ai` и панели, по приоритету. Ссылки `file:line` — по
состоянию кода на 19.09 вечер (сверены с кодом после СВ3).

## Состояние на 19.09, вечер

| Раздел | Состояние |
|---|---|
| СВ3 (лимиты, срез баз, выбор базы в чате, вкладка «Базы агента») | сделано; миграция `034` на БД уже применена |
| A. Регистрация базы | **сделано** (19.09, вечер): миграция `035`, маршруты, панель «Заявки → Подключение баз», вкладка «Чат в 1С» в карточке базы |
| B. Активация БИНов | **сделано** (19.09, вечер): миграция `036`, heartbeat, правило `activeBins`, панель «Заявки → Активация БИНов», активные БИНы в карточке агента |
| C. Много агентов | **сделано всё** (C0–C18), см. отметки; C11 — настройкой `ONEC_SERVER_SCOPE` (по умолчанию `all`, как было) |

Не закоммичено (задача запрещает git). Миграции `035`, `036` применятся при старте сервиса — после `pm2 restart all`.
Проверка: `ai` — `npm test` 305, `npm run verify`; SQL новых хранилищ — на живой БД в транзакции с откатом;
`frontend` — `npm run verify` (860 тестов). С живой 1С и живым агентом не проверялось.

Вторая волна (19.09, поздний вечер): `ai` — `npm test` 312, `verify`; SQL — на живой БД только чтением/с откатом.
- Предел параллельных команд по роли: `BUSINESS_IB_PARALLEL` (по умолчанию 4) — бизнес-агенту, `AGENT_IB_PARALLEL` —
  админ-агенту (`commands/queue.ts` `take(…, parallel)`, `agentRouter` `/poll`). Порядок «по одной на базу» прежний.
- C5: беседа не пишет агента; C8: `/admin/v1/commands` проверяет лимит, обход — `force: true` + аудит
  `command.limit_bypass`; C15/C16: сверка отметок агента с правилом сервиса на каждом полном срезе (аудит
  `agent.limit_mismatch` при повторе на двух срезах подряд, пометка «агент считает иначе» во вкладке «Базы агента»),
  `POST /v1/onec/active-bins/fix-all` и кнопка «Зафиксировать у всех агентов»; C17: срез — одной транзакцией, сбой не
  обрывает heartbeat; C18: БИН организации ERP — раз на диалог (`context.erpBin`), срезы — кэш 15 с.
- C9–C11: сервер — `?serverId=`, `serverId` тела или заголовок `X-Onec-Server` (панель шлёт его из выбора «Сервер 1С»,
  селектор виден при 2+ серверах; CORS разрешает заголовок). Без сервера одноимённая база — `409 BASE_AMBIGUOUS`
  (`r.param("key")` и `run()`), пакет пропускает её с причиной; «Обновить» опрашивает все видимые серверы
  параллельно (ответ `servers[]`); повтор задания — на сервер исходной команды; приём результатов — база на сервере
  агента. `ONEC_SERVER_SCOPE=organizations` — не суперадмин видит только серверы своих организаций (`/bases`,
  `/servers`, `/agents`, адрес базы и сервера).
- Сервер в расписании обслуживания (20.09): миграция `038`, поле `serverId` у расписания (пусто — не задан, как
  было), выбор сервера в форме показывается только при двух и более серверах, колонка «Сервер 1С» в списке
  расписаний; ночной и ручной прогон идут на сервер расписания, а без него одноимённые базы по-прежнему
  пропускаются с причиной.

### Что уже есть после СВ3 (на это опираться)

- `agents.max_bases`, `agents.max_bins`; таблица `agent_bases` (срез бизнес-агента в его порядке: transport,
  ext_version, over_limit, organizations jsonb) — `migrations/034_business_agent_bases_limits.sql`.
- `agents/agentBases.ts`: `evaluateLimits` (правило лимита), `resolveTarget` (выбор базы по `baseKey`/БИН),
  `describeAgentBases` (вид для панели), `AgentBasesStore.applySlice/list/listMany`.
- `AgentService.resolveBusiness(org, {baseKey, bin})` — агент + база или отказ `LICENSE_LIMIT`;
  `pickAgentFor` для бизнес-роли сначала ищет базу в `agent_bases`.
- `limits` в ответах register/heartbeat (`http/agentRouter.ts`); статус `OVER_LIMIT` в реестр `bases` не
  пишется (`forRegistry`).
- Чат ERP: `chat/workflow.ts` `targetBase()` → `execute()`; `baseKey` кладётся в payload; отказ по лимиту — без
  очереди, аудит `chat.license_limit`. `chat/orgBases.ts`: `baseKey` из `get_organizations` скрыт от модели и
  запомнен в `context.orgBases`.
- Панель: `GET /v1/onec/agents/:id/bases`, `PUT /v1/onec/agents/:id/limits` (только суперадмин; не названное
  поле сохраняет прежнее значение); вкладка «Базы агента» (`frontend/src/models/OneCAdmin/AgentBasesTab.tsx`).
- Подсказки `BASE_REQUIRED`, `BASE_NOT_FOUND`, `LICENSE_LIMIT`, `EXTENSION_MISSING` (`onec/errorHints.ts`).

## A. Регистрация базы 1С (Часть 1 контракта) — главное для клиентов

Состояние: **сделано.** `migrations/035_base_registrations.sql`, `bases/registrations.ts` (`RegistrationStore`),
`http/baseRegistrationRouter.ts` (монтируется до `onecChatRouter`), маршруты панели в `http/onecRouter.ts`
(`/registrations`, `/erp-organizations`, `/base-tokens`), панель `OneCAdmin/RegistrationsTab.tsx`,
`OneCBases/BaseChatTokens.tsx`. Тесты — `tests/base_registration.test.ts`, `src/__tests__/onecRequestsView.test.ts`.
Решения, которых нет в контракте: повтор заявки выдаёт новый `pollSecret`, прежний перестаёт действовать (два
действующих секрета — два получателя токена); число повторов и последний IP видны в панели. Прежние токены той же
базы 1С отзываются при выдаче нового. Одобрение без базы в реестре заводит её на сервере из заявки; одноимённые базы
на нескольких серверах — `409 BASE_AMBIGUOUS`, панель предлагает выбрать.

1. Таблица `base_registrations` (миграция **`035`** — `033` и `034` заняты): id, code (уникальный среди
   незакрытых, вида `K7M-42Q`), хэш `pollSecret`, тело заявки (base, organizations, user, contact, comment), IP,
   state, note, decided_by, decided_at, token_id, expires_at (7 дней).
2. `POST /v1/onec-chat/register` без токена, лимит 5/час на IP и `base.id`; повтор незакрытой заявки той же
   базы — та же заявка. Маршрут — в `http/onecChatRouter.ts` **до** проверки `X-Base-Token`.
3. `GET /v1/onec-chat/register/:id` с `X-Registration-Secret`; неверный секрет — 404. Токен — только при первом
   опросе после одобрения (`BaseTokenStore.issue`, `bases/tokens.ts:41`), дальше поля `token` нет; при повторной
   заявке неполученный токен отзывается (`revoke`, `bases/tokens.ts:55`).
4. Панель «Заявки на подключение»: список с поиском по коду, «Одобрить» (организация ERP — по совпавшему БИН,
   ключ базы — `base.name`; создать или найти строку `bases`), «Отклонить» с причиной. Аудит `base.registration.*`.
5. Карточка базы: её токены (кем, когда, отозвать). В `bases/tokens.ts` уже есть `issue`/`revoke`/`list`
   (`:41`, `:55`, `:60`), но HTTP-маршрутов нет — вызывается только `resolve` (`server.ts`, `onecChatRouter`).

6. **(добавлено 19.09, аудит РС10)** `POST /v1/onec-chat/revoke` с `X-Base-Token` — база отзывает свой токен сама
   («Отключить базу» в 1С); ответ `{revoked: true}`. Контракт — часть 1.
7. **(добавлено 19.09, аудит РС2)** 1С теперь проверяет сертификат сервиса по корневым сертификатам ОС и не
   ходит на сервис по `http` (кроме localhost): у `ai.buhprof.kz` должна быть действительная цепочка сертификатов.

8. **(добавлено 19.09) Подключение АГЕНТА по коду** — `POST /agent/v1/enroll`, `GET /agent/v1/enroll/:id`
   (`X-Enrollment-Secret`), в панели «Агенты» → «Подключить по коду». Контракт —
   [CONTRACT_AGENT_ENROLLMENT_2026-09-19.md](CONTRACT_AGENT_ENROLLMENT_2026-09-19.md). Агент (окно, «Подключить
   агента по коду…») готов; пока маршрута нет, окно отвечает «возьмите идентификатор и токен в панели».

## B. Активация БИНов (Часть 2 контракта)

Состояние: **сделано.** `migrations/036_bin_activation.sql`, `agents/activation.ts` (`ActivationStore`),
`activationRequests` в heartbeat и `limits.activeBins` + `activation` в ответах register/heartbeat
(`http/agentRouter.ts`), правило в `evaluateLimits` (отказ «не активирована, запросите активацию»), маршруты
`/activation-requests/*` и `PUT /agents/:id/active-bins`, панель `OneCAdmin/ActivationRequestsTab.tsx` и блок
активных БИНов во вкладке «Базы агента». Первое одобрение у агента без списка заводит список из обслуживаемых
сейчас БИНов плюс новый; сверх `maxBins` — одобряется с предупреждением.

1. `agents.active_bins` (text[] NULL — «списка нет, правило первых N»), таблица `bin_activation_requests`
   (agent_id, bin, name, base_key, comment, state, note, decided_by, decided_at). Миграция — после A (`036`).
2. Heartbeat (`heartbeatSchema`, `http/agentRouter.ts`): принять `activationRequests` (upsert по agent_id+bin);
   в ответах register/heartbeat отдавать `limits.activeBins` (рядом с уже отдаваемыми `maxBases`/`maxBins`) и
   `activation` (решения за 90 дней).
3. `evaluateLimits` (`agents/agentBases.ts:75-110`): при `active_bins` — обслуживаются ровно они; текст отказа в
   `resolveTarget` — «не активирована, запросите активацию». `describeAgentBases` и вкладка «Базы агента» —
   статус «Активен / Не активен» по тому же правилу. Тесты — `tests/agent_bases_limits.test.ts`.
4. Панель: «Запросы активации БИНов» (одобрить → в `active_bins`; предупредить при превышении `maxBins`;
   отклонить с причиной), в карточке агента (вкладка «Базы агента») — активные БИНы с «Отключить» и
   «Зафиксировать текущие» (по `describeAgentBases`: БИНы без пометки `overLimit`).

## C. Много бизнес- и административных агентов — найденные пробелы

Порядок — по вреду. C0 и C15–C18 найдены аудитом СВ3. ✔ — сделано 19.09 вечером:
- C0: память «id → база» (`context.idBases`), адрес `organizationId` у поиска и чтения (`tools/registry.ts`
  `ROUTING_ORGANIZATION`, в 1С не уходит), отказ `MIXED_BASES` без очереди, правило 19 в системном промпте;
- C1: `baseKey` в `enqueue`; учётная запись базы при выдаче — только админ-агенту (`server.ts`, `setAuthResolver`);
- C2: без адреса у агента с несколькими базами — `BASE_REQUIRED` сервисом; обзорные команды идут без адреса;
- C3: отказ по лимиту у одного агента не мешает другому; агент диалога — первым (`context.baseAgents`);
- C4: в `orgBases` первая строка выигрывает; C6: статус базы из среза (`OFFLINE` → `ONEC_UNAVAILABLE`);
- C7: `organizationBin` в payload, когда база выбрана по БИН организации ERP и у инструмента он есть;
- C12, C13: `/v1/agents` — роль, базы, лимит; `/v1/status` и «не на связи» — только бизнес-агенты;
- C14: предупреждение в журнале при `AGENT_ORG_BINDING=any` и `NODE_ENV=production`.

Бизнес-команды:

0. ✔ **Команды одного действия расходятся по разным базам** (аудит СВ3, главное). База выбирается на каждый вызов
   отдельно (`chat/workflow.ts` `targetBase`): вызов без организации (`search_counterparties`, `search_products`)
   идёт в базу организации ERP, вызов с `organizationId` — в базу этой организации. «Реализация от ТОО Бета»:
   контрагент найден в базе «Альфа», документ создаётся в «Бете» с чужим id → 1С откажет или возьмёт не тот
   объект. Тест `tests/chat_agent_bases.test.ts` этого не ловит (заглушка не сверяет id). Сделать: запоминать
   базу каждого увиденного id (`collectIds` в `interpret`, рядом с `seenIds`), выбирать базу по id, на которые
   ссылается вызов; id из разных баз в одном вызове — ошибка модели с понятным текстом.
1. ✔ **baseKey только в payload, не в колонке очереди** — `queue.enqueue` без `baseKey` (`chat/workflow.ts:488-493`):
   `commands.base_key = NULL` (`queue.ts:253-254`), нет сериализации по базе и `ibParallel` (`queue.ts:505-537`),
   dedupe по `(agent_id, '', request_id)` (`queue.ts:264`). Передавать `baseKey` в `enqueue`.
   **Осторожно:** при заполненном `base_key` выдача подставляет в payload учётную запись администратора базы
   (`queue.ts:562-575`, `setAuthResolver` в `server.ts:107-111`, по серверу агента). Бизнес-агенту она не нужна —
   ограничить `authResolver` ролью `admin` (или типами команд) в той же правке.
2. ✔ **Путь `none` → `pickOnline` без лимита** (`workflow.ts:466`): команда уходит первому онлайн-агенту без
   проверки тарифа и без `baseKey`. Отказывать с `BASE_REQUIRED`, если у агента в срезе несколько баз, а базу
   определить нельзя. Сюда же — устаревший запомненный `baseKey` (базу убрали из настроек агента): сейчас он
   даёт `none`, выбор по БИН не пробуется.
3. ✔ **2+ бизнес-агента с одним БИН/ключом базы** — берётся первый (агенты на связи — первыми, дальше порядок
   `listByOrganization` по `created_at`; `agentBases.ts:145-195`), при отказе по лимиту у первого другой не
   пробуется (`agentBases.ts:157-189`). Карта `orgBases` без agentId (`chat/orgBases.ts:37-38`) — хранить пару
   «агент + база».
4. ✔ **Один БИН в нескольких базах**: `orgBases` берёт последнюю строку ответа (`chat/orgBases.ts:38` перезаписывает),
   `resolveTarget` — первую обслуживаемую по срезу. Одно правило на оба места (первая обслуживаемая по порядку).
5. ✔ **Беседа привязана к одному агенту** (`workflow.ts:799-803`) — уточнено: `conversations.agent_id` только
   записывается при создании и больше нигде не читается; агент выбирается на каждый вызов. Вред — неверная
   справка (и FK при удалении агента, `agents/service.ts:193`), а не маршрутизация. Низкий приоритет: либо не
   писать, либо обновлять последним исполнителем.
6. ✔ **Доступность 1С — на агента, не на базу** (`agentRouter.ts` heartbeat → `agents.onec_reachable`,
   `workflow.ts:477`): у многобазового агента брать `status` базы из `agent_bases` (`ONLINE/OFFLINE/OVER_LIMIT`).
7. ✔ **БИН организации ERP не уходит в payload** (`workflow.ts:519-526`; в канале 1С — есть: `withOrganization`
   `workflow.ts:940-945`) — класть `organizationBin` у инструментов, где он есть в схеме, иначе в базе с
   несколькими организациями 1С выберет сама. Для однобазовых агентов старых сборок — проверить, что 1С не
   откажет по чужому БИН.
8. ✔ `/admin/v1/commands` (`adminRouter.ts:77-94`) ставит любую команду в обход лимита. Уточнение: маршрут закрыт
   сервисным ключом `AGENT_ADMIN_KEY` (`adminRouter.ts:38`), а не правом пользователя — это служебный вход.
   Решить: проверять лимит (`resolveBusiness`) для бизнес-команд или явно описать как обход.

Административные:

9. ✔ **«Один сервер на установку»** (`agents/service.ts:328-336`): кластерные команды без базы — первому
   админ-агенту (`admin.ts:129-130,139,174`), `/bases/refresh` обновляет один сервер (`onecRouter.ts:461-475`).
   Нужен выбор сервера (serverId) в API панели и обход всех админ-агентов для «обновить всё».
10. ✔ **Ключ базы на нескольких серверах**: `findByKeyGlobal` `LIMIT 1` (`bases/service.ts:690-699`), агент
    выбирается независимо от базы (`onecRouter.ts:238-249`), пакеты и повторы — только по ключу
    (`batchRunner.ts:122`, `onecRouter.ts:1446`). Адресовать базу парой «сервер + ключ».
11. ✔ **Админ-агенты не фильтруются по организации** (`service.ts:338-339` `pickAdminAgent` → `listAll`) —
    серверы разных клиентов смешиваются.

Общие:

12. ✔ `GET /v1/status` показывает одного агента и смешивает роли (`userRouter.ts:62-77`); `GET /v1/agents`
    (`userRouter.ts:47-57`) — без роли, баз, лимитов. Нужны списки по ролям с базами (данные — `agent_bases`,
    `describeAgentBases`).
13. ✔ Сообщение «агент не на связи» считает и админ-агентов (`workflow.ts:470`: фильтр только `!disabled`).
14. ✔ (предупреждение при старте в production) В режиме `any` выбор уходит к агентам чужих организаций (`service.ts:274-276` — `pickAgentFor`,
    `:312-315` — `resolveBusiness`) — оставить только для разработки явно.
15. ✔ **Правило счёта БИНов не сверено с агентом** (аудит СВ3): сервис считает БИНы только в базах в пределах
    лимита (`evaluateLimits`, база сверх лимита не занимает место организации). Сверить с кодом агента; с
    `activeBins` (B) вопрос снимается для активированных агентов.
16. ✔ Признак `overLimit` самого агента сервис при выборе базы не учитывает — только своё правило (по задаче СВ3
    главное). При расхождении правил агент ответит `LICENSE_LIMIT`; панель показывает обе пометки.
17. ✔ Срез пишется двумя запросами без транзакции (`AgentBasesStore.applySlice`), ошибка в нём обрывает heartbeat.
    Низкий риск после дедупликации ключей; при правке B — обернуть в транзакцию.
18. ✔ На каждый вызов инструмента чата — три запроса (БИН из ERP, агенты организации, их срез). Кэшировать БИН
    организации на диалог, если станет заметно.

## D. Подключение агента по коду (СВ5) и управление агентами из панели — сделано (19.09, ночь)

Контракт — [CONTRACT_AGENT_ENROLLMENT_2026-09-19.md](CONTRACT_AGENT_ENROLLMENT_2026-09-19.md). Что требует доработки
агента (перезапуск, обновление, настройки, журнал и процессы бизнес-агента) — отдельной задачей
[TASK_AGENT_REMOTE_CONTROL_2026-09-19.md](TASK_AGENT_REMOTE_CONTROL_2026-09-19.md).

1. ✔ Подключение по коду: `POST/GET /agent/v1/enroll`, панель — «Агенты → Подключить по коду», «Заявки →
   Подключение агентов»; одобряет администратор BuhProf, аудит `agent.enrollment.*`.
2. ✔ Сводка бизнес-агента — его команда `HEALTH` (`GET /agents/:id/health` по роли); вкладка «Журнал агента» у
   бизнес-агента скрыта, а не падает.
3. ✔ Карточка агента → «Команды»: очередь, выполняемые, итоги; «Снять с очереди» для не начатых
   (`GET /agents/:id/commands`, `POST /commands/cancel`).
4. ✔ Карточка агента → «Журнал действий» (`GET /agents/:id/audit`, имена пользователей из ERP).
5. ✔ «Не на связи»: сводка над списком агентов (со временем пропажи) и число у вкладки «Агенты».
6. ✔ Список агентов: отборы по роли и состоянию, колонки ОС, «1С доступна», базы бизнес-агента, счётчики команд;
   групповое «Отключить» и «Включить» по отмеченным (перевыпуск токенов группой — нет: каждый токен вписывают на своём
   компьютере).
7. ✔ Сервис отдаёт и хранит, что раньше терял: ОС, доступность 1С, дата регистрации, организация, версия экземпляра,
   `commandsDone`/`commandsFailed` из heartbeat (миграция `037`).
8. ✔ Снятие процесса — у выбранного агента (`agentId`), колонка «Агент» в «Процессах»; действия по `/agents/:id/*`
   проверяют видимость агента (при `ONEC_SERVER_SCOPE=organizations`).

## Готово, когда

- база из 1С отправляет заявку, в панели видно код и БИНы, после «Одобрить» чат в 1С работает без ручного
  токена;
- агент запрашивает активацию БИНа из окна, в панели запрос одобряется, в окне агента статус «Активен»;
- два бизнес-агента одной организации с разными базами и один админ-агент на каждом из двух серверов: команды
  чата уходят в нужную базу нужного агента, списки кластера — нужному серверу;
- диалог «найди контрагента → создай документ» по организации второй базы: оба вызова уходят в одну базу (C0).

Проверка без живых агентов: `bpapi_agent/tools/mock_cloud.py --active-bins …` и `POST /ops/activation`,
`buhprof_api/tools/mock_chat_service.py` (регистрация).

Ограничения: git не использовать; пароли и токены в журнал не писать; публиковать только на localhost.
