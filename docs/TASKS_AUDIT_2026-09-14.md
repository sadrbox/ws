# Аудит согласованности панели, сервиса и агента — задачи

> 14.09.2026. Сверены каталог команд и payload (30 общих типов), ответы, коды ошибок, сроки и
> эхо состояния. Здесь только расхождения, подтверждённые чтением обеих сторон. Приоритет:
> **В** — высокий, **С** — средний, **Н** — низкий. Уже заведённое не повторяется:
> `TASK_SERVICE_SHOW_IN_LIST.md` (S1–S3, P1–P2), `TASKS_DEV_2026-09-14.md` (R1–R7).
> Параллельный список сессии сервиса — `TASKS_ONEC_AUDIT_2026-09-14.md` (T1–T8); общие пункты
> сопоставлены в конце.

## Статус

**Правило: после каждого исправления отметить пункт здесь — сборкой агента или коммитом.** Пока
в «Осталось» есть пункты, документ не закрыт. Сборка агента на сервере — `2026-09-15 00:32` (15.09 00:42: А12 подтверждён проверкой
базы); до неё `00:12` (А11 подтверждён 00:23), `23:52` (А9, А10 подтверждены 15.09 00:05), `23:16`
(окно агента; версию службы сверить в панели). **14.09 23:27 (+05) — самопроверка повторена на 23:16:
18 из 18.** **14.09 22:49 (+05) — самопроверка `_transition` из
окна агента: 18 шагов из 18** — роли всеми способами, показ в списке выбора в обе стороны, запреты.

| Часть | Сделано | Осталось |
|---|---|---|
| Агент | А1–А5 — сборка `2026-09-14 22:05`; `22:38` — плюс «Проверить операции в базе» в окне (на сервере, самопроверка пройдена); **`23:16` — А6, А8 (контракт), А7 частично (признак `active`, `warning`, защита блокировки при загрузке и обновлении), самопроверка в «Отладочной информации»** (на сервере с 23:27, самопроверка 18 из 18); **`23:52` — А7 (сброс незаданного по ступеням), А8 (`IB_BATCH` не объявляется), А9 (BOM), А10 (обрыв связи с рабочим процессом)** (на сервере с 15.09 00:05, А9 и А10 подтверждены); **`2026-09-15 00:12` — `cluster.locks` в `AGENT_HEALTH` (покрытие чтения блокировок), А11 (пределы рабочего сервера в тексте обрыва)** (на сервере с 15.09 00:23, А11 подтверждён); **`2026-09-15 00:32` — А12 (вывод «процесс, вероятнее всего, аварийно завершился» при нулевых пределах), «Отладочная информация» с отметкой «с проверкой баз данных»** (на сервере; А12 подтверждён 15.09 00:42) | установить 00:32; прислать «Отладочную информацию» с отметкой «с проверкой баз данных» — по причинам отказов уточнить распознавание входа в СУБД и в базу (`ib/ibcmd.rs`); на `_transition` проверить сброс окна блокировки (А7) и А2; строка сервиса «полный срез баз: строк с блокировкой сеансов» > 0 (А6), мало — смотреть `cluster.locks` в «Состоянии сервера»; А1 — проверка базы обрывается: все пределы кластера и рабочего сервера 0, а `rphost` перезапущен — вероятно, падает сам; на сервере — журнал «Приложение» Windows около 00:23 (сбой `rphost.exe`), дампы рабочего процесса, проверка на копии базы, затем повторить; T8 с администратором |
| Сервис | С9 (их T3), С8 — сессией сервиса (`commandRequestId`); **С1–С7, С10–С14 — сессией сервиса 14.09** (миграция `025_command_queue_slots.sql`, нужен `pm2 restart all`); R1–R5 и эхо А7 (`lock.active`, `warning`, подсказка `SESSIONS_LOCK_NOT_ACTIVE`) — `fc67c9f`, `89e9b4c` | **С16 — открыт (15.09 00:32; 00:42 на агенте 00:32 подсказка по-прежнему советует поднять пределы — сразу после вывода агента «пределы не заданы»):** подсказка к `IB_CONNECTION_LOST` советует «поднимите пределы в консоли кластера» и тогда, когда агент показал, что пределов нет (сервер 15.09 00:23: все пределы 0, процесс перезапущен). Совет про пределы — только если в тексте агента нет вывода «пределы перезапуска и памяти не заданы»; иначе — «процесс упал: журнал «Приложение» Windows, дампы `rphost`, проверка на копии базы» (`ai/src/onec/errorHints.ts`). **С15 — сделано 15.09** (подсказка; причина «в базу не войти» по этому коду не ставится). Было: подсказка к новому коду агента `IB_CONNECTION_LOST` (23:52) в `errorHints.ts` — «рабочий процесс кластера упал или перезапущен; текст агента называет перезапущенные процессы и пределы памяти» |
| Панель | П1–П9 — сессией панели 14.09 (подробности — `TASKS_ONEC_AUDIT_2026-09-14.md`) | **П10 — сделано 15.09** (предупреждение с `note`, если `reset` не `all`). Было: после «Закрыть вход» показывать `note` из ответа `CLUSTER_SET_SESSIONS_LOCK`, если `reset` не `all` (агент 23:52: прежние окно, сообщение или код не сброшены); **П11 — сделано 15.09** (строка «Чтение блокировок сеансов» и последний отказ). Было: в «Состоянии сервера» показать `cluster.locks` из `AGENT_HEALTH` — «Блокировки: прочитано N из M, включены у K, на паузе после отказа P; последний отказ: база — причина» (агент после 23:52); **П12 — сделано 15.09** (сервис: `warning` в строке задания; панель: итог операции — предупреждение). Было: после правки или создания пользователя показывать предупреждение, если в ответе агента `unverified: ["showInList"]` (признак не перечитан) или `skipped` (платформа не приняла необязательные свойства, например полное имя) — сейчас это теряется; сервису — сохранить эти поля в результате элемента задания |

## Агент

| № | П | Задача |
|---|---|---|
| А1 | В | **`IB_CHECK` не проверяет базу.** В конфигуратор уходят `-ReIndex -LogIntegrity -BadRefs -Verify` без `/IBCheckAndRepair`, а `-BadRefs`/`-Verify` — не ключи 1С (`ib/mod.rs`, `check`). Без флагов включается `-ReIndex`, который меняет базу. **Сделано 22:05** |
| А2 | В | **`IB_RESTORE` через `ibcmd` обходит `lockSessions`.** Быстрый путь (`try_ibcmd`) шёл раньше `with_sessions_locked`: с паролем СУБД база заменялась при работающих пользователях. **Сделано 22:05** |
| А3 | Н | **`IB_UPDATE_USER` с `fullName: ""` молча не очищал имя** — мост проверял непустую переменную. **Сделано 22:05** |
| А4 | Н | **`IB_BATCH` расходился с групповыми заданиями сервиса** (`bases` против `baseKeys`, `dryRun` сверху не действовал). **Сделано 22:05** (общий предел на пакет остаётся — сервис `IB_BATCH` не использует) |
| А5 | Н | План сухого прогона `IB_UPDATE_USER` не видел `addRoles`/`removeRoles`. **Сделано 22:05** |
| А6 | В | **Блокировка сеансов не доходит до реестра (их T1): у 111 баз из 111 состояние пустое.** Причина подтверждена кодом: полный срез в heartbeat собирает `collect_bases` (`agent.rs`), которая переносит из строки кластера только перечисленные поля в `BaseState` (`cloud/protocol.rs`) — поля `lock` там нет. `lock` доходит только в ответе `CLUSTER_LIST_INFOBASES`. Сделать: `lock` в `BaseState` и в `collect_bases`. После сборки — строка сервиса «полный срез баз: строк с блокировкой сеансов»; если строк мало — смотреть отказы фонового чтения (пауза 6 ч на базу, учётная запись `cluster.infobase_user` из настроек или секретов службы). **Сделано 23:16** (`BaseState::from_cluster_item`, тест) |
| А7 | С | **Окно блокировки не стирается (их T5).** `set_sessions_lock` передаёт `--denied-from/--denied-to/--denied-message/--permission-code` только непустыми, а `rac infobase update` меняет лишь переданное: прежние окно и сообщение остаются. Хуже всего для обслуживания: `IB_RESTORE`/`IB_APPLY_UPDATE` ставят блокировку без окна, и старое прошедшее окно делает её недействующей — пользователи входят посреди загрузки. Сделать: при включении явно задавать или сбрасывать окно; сначала на `_transition` выяснить, чем `rac` сбрасывает дату (пустое значение или иное). **Частично 23:16**: `lock.active` (действует ли сейчас), `warning` у `CLUSTER_SET_SESSIONS_LOCK`, загрузка и обновление добиваются действующей блокировки (окно «сейчас — сутки», иначе отказ `SESSIONS_LOCK_NOT_ACTIVE`). **Сделано 23:52**: при включении незаданное сбрасывается по ступеням — окно нулевой датой 1С, сообщение и код разрешения пустыми; при отказе `rac` — только окно, затем прежний вызов; в ответе `reset` (`all`/`dates`/`none`) и `note`. **Проверить на `_transition`**: включить блокировку без окна после блокировки с окном — ожидается `reset: "all"`, `state.lock.from`/`to` — `null`, `active: true`. **Сервис и панель 14.09 (`89e9b4c`):** `lock.active` хранится (миграция 026) и показывается «Закрыт, но не действует»; `warning` у `CLUSTER_SET_SESSIONS_LOCK` — предупреждением после «Закрыть вход»; к `SESSIONS_LOCK_NOT_ACTIVE` — подсказка |
| А8 | Н | **Контракт (их T7).** `ONEC_AGENT_CLUSTER_CONTRACT.md` описывает эхо только `users`/`extensions`/`sessions`/`connections` — дописать `state.lock`, `infobases`, `config`, `processes`, `publication`, `locks`. По `IB_SELFTEST`, `AGENT_LOG_TAIL`, `AGENT_HEALTH`, `IB_BATCH` — **решение**: описать для сервиса и панели или перестать объявлять. **Контракт — сделано 23:16** (все блоки эха, служебные команды, коды `IB_FIELD_NOT_APPLIED` и `SESSIONS_LOCK_NOT_ACTIVE`). **Решение 14.09 (сессия сервиса и панели):** `AGENT_HEALTH`, `AGENT_LOG_TAIL`, `IB_SELFTEST` — оставить: сервис и панель их используют (`TASKS_DEV_2026-09-14.md`, R1, R2, R4); `IB_BATCH` — перестать объявлять. **Сделано 23:52**: `IB_BATCH` не объявляется (исполняется только расписанием агента, тест), в контракте записано решение |
| А9 | Н | **BOM в тексте отказа конфигуратора.** Отчёт `/Out` пишется в UTF-8 с BOM, `cluster::decode` его не убирает — сообщение начинается невидимым `﻿` («проверка базы не выполнена: ﻿Тестирование начато…», сервер 14.09). Сделать: убирать `\u{feff}` при чтении отчётов конфигуратора и вывода утилит. **Сделано 23:52** (`cluster::decode`, тест). **Проверено на сервере 15.09 00:05** — текст отказа без BOM |
| А10 | С | **Обрыв связи с рабочим процессом во время операции конфигуратора не объяснён.** 14.09, `IB_CHECK` на `_transition`: «Тестирование начато» (ключи А1 приняты), затем `server_addr=tcp://SERVER:1560 descr=10054 … forcibly closed` — рабочий процесс кластера упал, перезапущен или оборвал вызов; агент отдаёт это как `IB_ERROR` сырым текстом. Сделать: на таком отказе агент читает `rac process list` (время запуска rphost — перезапуск во время операции) и настройки кластера (`lifetime-limit`, `max-memory-size`, `max-memory-time-limit`, `kill-problem-processes`) и называет вероятную причину; отдельный код отказа (например `IB_CONNECTION_LOST`). **Сделано 23:52**: код `IB_CONNECTION_LOST` у конфигуратора, загрузки из выгрузки и COM-моста; к тексту — рабочие процессы, запущенные после начала операции, и настройки перезапуска и памяти кластера (`connection_hint`, тесты). **Проверено на сервере 15.09 00:05**: отказ пришёл кодом `IB_CONNECTION_LOST`, без BOM, с перезапущенным процессом (SERVER:1560, запущен во время проверки) и настройками кластера |
| А11 | С | **Подсказка к обрыву не видит пределов рабочего сервера.** 15.09: у кластера `max-memory-size` = 0 и `lifetime-limit` = 0, правило кластера по памяти не действовало — а процесс всё равно перезапущен. Снять его мог рабочий сервер: «критический объём памяти процессов», «временно допустимый объём», «безопасный расход памяти за один вызов» задаются в `rac server list`, а не в кластере. Сделать: добавить эти поля в подсказку и пояснять нули («0 — без ограничения»). **Сделано `2026-09-15 00:12`** (`connection_lost_hint` читает `rac server list`, тест). **Проверено на сервере 15.09 00:23** — пределы рабочего сервера пришли в тексте |
| А12 | Н | **Вывод при нулевых пределах и `bases check` в отладочной информации.** 15.09 00:23: процесс перезапущен, все пределы кластера и рабочего сервера 0, а текст этого не объяснял — и подсказка сервиса советовала «поднимите пределы». Сделать: агент дописывает вывод «пределы не заданы — процесс, вероятнее всего, аварийно завершился: журнал «Приложение» Windows, дампы»; «Отладочная информация» — с отметкой «с проверкой баз данных» (отчёт `bpapi-agent bases check`: нужны реальные причины отказов входа, чтобы уточнить их распознавание). **Сделано `2026-09-15 00:32`** (`explain_connection_lost` — вывод, только если процесс перезапущен и прочитаны пределы и кластера, и рабочего сервера; `diag::bases_check_report` — тот же путь, что `CLUSTER_CHECK_BASES`, с секретами службы; тесты). Сервису — С16 (раздел «Статус»). **Проверено на сервере 15.09 00:42** — вывод пришёл в тексте обрыва (упали два процесса, SERVER:1561 и SERVER:1560) |

**Что именно сделано в агенте (`2026-09-14 22:05`):**

* **А1.** Режим `/IBCheckAndRepair`. Без `repair` — осмотр: `-LogAndRefsIntegrity -TestOnly`;
  переиндексация и пересчёт итогов меняют базу, поэтому не выполняются и приходят в
  `skipped: ["reindex", "recalcTotals"]`. С `repair` — выбранное плюс `-BadRefCreate -BadDataCreate`
  (восстановление созданием объектов, не удалением); `-Rebuild` больше не ставится. В ответе
  `keys` — с какими ключами шёл конфигуратор; строки «ошибок не обнаружено» ошибкой не считаются.
  План сухого прогона называет ключи и то, что без «Исправлять» не выполнится.
* **А2.** Блокировка входа и ожидание выхода пользователей — снаружи обоих путей загрузки
  (`ibcmd` и конфигуратор), одна на команду.
* **А3.** `fullName: ""` очищает полное имя (маркер «поле пришло»). Пустой пароль — по-прежнему
  «не трогать».
* **А4.** `IB_BATCH` принимает `baseKeys` так же, как `bases`; `dryRun` пакета доходит до каждой
  базы. Без списка — по-прежнему все базы кластера (так пакет задаёт расписание агента).
* **А5.** План `IB_UPDATE_USER` пишет «снять роли: …», «выдать роли: …».
* Попутно: живой мост (`agent.persistent_bridge`) очищает `ADD_ROLES`/`REMOVE_ROLES` между
  командами — поправки ролей одной правки не переходят на следующего пользователя.

## Сервис

| № | П | Задача |
|---|---|---|
| С1 | В | **Кластерные команды с `baseKey` встают во внутрибазовую очередь.** `onecRouter.ts:211` пишет `base_key` любой команде, `queue.ts:375-412` считает такие «внутрь базы»: снятие сеанса, блокировка входа, удаление регистрации ждут выгрузку той же базы. Тупик: `IB_RESTORE` 120 с ждёт выхода пользователей, а снятие их сеансов ждёт `IB_RESTORE`. Сделать: команды `cluster.admin` не занимают место и очередь базы **Сделано**: признак `in_base` (`runsInsideBase` — только `ib.admin`); кластерные с базой не занимают место и очередь базы |
| С2 | В | **Срок жизни считается от постановки, а место одно** (`queue.ts:134`, `AGENT_IB_PARALLEL=1`). Групповое задание ставит все базы разом (`batchRunner.ts:109-116`): не дождавшиеся места истекают с текстом «служба 1С-агента не на связи». Сделать: срок выполнения — от выдачи агенту; ожиданию в очереди — свой предел и свой текст причины **Сделано**: срок выполнения (`ttl_seconds`) — от выдачи; ожидание очереди — 900 с одиночной, 12 ч заданию; истечение в очереди — `COMMAND_QUEUE_TIMEOUT` «агент был занят» |
| С3 | С | **Истёкшая выданная команда освобождает место, пока агент ещё работает** (`queue.ts:251-270`, 375-380): агент получает вторую внутрибазовую команду поверх первой. Сделать: место занято, пока нет результата или не вышел предел самого агента (срок ≥ предел агента + запас) **Сделано**: истёкшая выданная без результата держит место ещё 600 с |
| С4 | С | **Проверка базы с `repair: true` доступна «только просмотр»** — `/bases/:key/check` нет в `DESTRUCTIVE` (`access.ts:47`). Сделать: отказ без `canOnecWrite` при `repair` **Сделано**: `isDestructive` — `/check` при `repair: true` |
| С5 | С | **`dryRun`**: успешный сухой прогон снимает отметку «в базу не войти» (`agentRouter.ts:651-662`), и он идёт общей очередью с долгим сроком (подтверждение ждётся до 15 мин). Сделать: не вызывать `markIbReachable` при `dryRun`; короткий срок и приоритет **Сделано**: `marksReachability` без `dryRun`; `dryRun` — срок 900 с, приоритет −1 |
| С6 | С | **`IB_AUTH_FAILED` не превращается в `NO_ACCESS`** — причина ищется по тексту (`bases/service.ts:207-235`). Сделать: сопоставлять по коду **Сделано**: `IB_AUTH_FAILED` → `NO_ACCESS` по коду |
| С7 | С | **`CLUSTER_LIST_PUBLICATIONS` после 202 отдаёт сырой ответ агента** (`onecRouter.ts:996` против преобразования в 314-343): панель кладёт строки публикаций в список баз **Сделано**: `GET /commands/:id` для публикаций отвечает реестром и `report`, как сразу; панель берёт его |
| С8 | С | **Изменяющие команды без `requestId`** (ставится только чтениям, `onecRouter.ts:229-231`): повтор после «слишком долго» ставит вторую загрузку или обновление. Сделать: `requestId` и для изменяющих команд из панели **Сделано** сессией сервиса (`admin.commandRequestId`) |
| С9 | Н | `AGENT_CANCEL_COMMAND`: `state.processes` не применялся. **Сделано** сессией сервиса (их T3, `writeState.planWriteState`) |
| С10 | Н | `IB_BUSY` не повторяется ни заданием, ни обслуживанием по расписанию (агент прямо советует повторить эти базы) **Сделано**: `retryBusy` — копия в том же задании, до 3 попыток; повторённая в отчёт не входит |
| С11 | Н | `waitResult` не считает `canceled` конечным (`queue.ts:498`) — лишние 20 с ожидания **Сделано** |
| С12 | Н | Расписание сохраняется без проверки payload по схеме и может хранить `password` (`onecRouter.ts:846-851`) **Сделано**: `validateSchedulePayload` — схема команды, без `password`/`contentBase64`/`auth` |
| С13 | Н | Контракт устарел: срок 300 с и ожидание 120 с (`ONEC_AGENT_CLUSTER_CONTRACT.md:34-37`), кода `TIMEOUT` агента нет в текстах; `IB_CHECK` в контракте не описан (ответ теперь с `keys`, `skipped`) **Сделано**: сроки §1, `IB_CHECK`, `TIMEOUT`/`IB_AUTH_FAILED` в кодах, очередь; подсказка к `TIMEOUT` |
| С14 | Н | `IB_PUBLISH`/`IB_UNPUBLISH`: схема не пропускает `apache22`, агент его умеет — если нужен **Сделано**: `apache22` |

## Панель

| № | П | Задача |
|---|---|---|
| П1 | В | **Общая форма «изменить пользователя» переписывает права и доступ во всех базах.** `ElementForm.tsx:146-155` шлёт `roles` целиком (взяты из первой базы, где они есть) и `disabled` всегда, пакетом по отмеченным базам. Сделать: только изменённое, роли — `addRoles`/`removeRoles`, как в `userUpdate.ts` **Сделано** (`userUpdate.buildGroupUserUpdate`) |
| П2 | В | **Долгие операции ждутся 15 минут** (`api.ts:100`, `awaitCommand`) — `IB_RESTORE`, `IB_APPLY_UPDATE`, `IB_CHECK` идут до 4 ч: операция показывается упавшей, номер команды теряется, повтор ставит вторую. Сделать: для долгих — переход в «Прогресс» по `commandId` без предела и запрет повтора, пока команда жива **Сделано** (`followCommand`, `workKey`; повтор склеивает С8) |
| П3 | С | **Решение о `force` по тексту, а не по коду** (`ProcessesTab.tsx:91-99`): `AGENT_PROCESS_NOT_FOUND` не предлагает `force`, хотя агент его принимает. Сделать: по `AiServiceError.code` **Сделано** (по `AiServiceError.code`) |
| П4 | С | `CLUSTER_DISCONNECT`: применять `state.locks`. **Сделано** сессией панели (их T2, `ConnectionsTab`, `echoList(…, "locks")`) |
| П9 | С | **Проверка базы: переиндексация и пересчёт итогов — только вместе с «Исправлять».** Агент 22:05 без `repair` их не выполняет и возвращает `skipped`; панель по умолчанию ставит переиндексацию (`BaseMaintenance.tsx:46`). Сделать: эти переключатели доступны при «Исправлять», итог показывает `skipped` **Сделано** (только с «Исправлять», `skipped` в итоге) |
| П5 | Н | `AGENT_CANCEL_COMMAND`: ответ 202 читается как «прерывать нечего» (`api.ts:399-403`, `BatchesTab.tsx:204`) — ждать результат **Сделано** (`abortCommand` ждёт ответ) |
| П6 | Н | `CLUSTER_CHECK_BASES`: показывать `items[].reason` — какие базы не проверены и почему **Сделано** (`checkDbOutcome`: непроверенные с причиной) |
| П7 | Н | Публикации после 202 — перечитать реестр, а не класть ответ в кэш баз (пара к С7) **Сделано** (после 202 — `fetchBases`) |
| П8 | Н | `BatchType` объявляет `IB_LIST_USERS`/`IB_LIST_EXTENSIONS`, которых `/batch` не принимает **Сделано** (типы убраны из `BatchType`) |

## Связь со списком сессии сервиса (`TASKS_ONEC_AUDIT_2026-09-14.md`)

| Их пункт | Здесь | Статус |
|---|---|---|
| T1 блокировка из среза баз | А6 | сервис — сделано (журнал покрытия); агент — причина найдена, не исправлено |
| T2 `state.locks` в панели | П4 | сделано |
| T3 `state.processes` при отмене | С9 | сделано |
| T4 чтение пользователей после обновления без эха | — | сделано сервисом |
| T5 окно блокировки | А7 | агент — не исправлено; панель — после агента |
| T6 история операций в панели | — | сделано панелью |
| T7 контракт и 4 команды | А8 | не сделано, нужно решение |
| T8 проверка E1–E6 на сервере | — | ждёт; ставить после сборки агента с А6 и А7 |

## Не проверено

Формы строк сеансов, соединений, блокировок, лицензий и рабочих процессов (`ClusterRow`);
реальные длительности на сервере.
