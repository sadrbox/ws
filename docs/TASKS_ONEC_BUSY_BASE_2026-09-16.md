# База занята фоновым заданием: задачи по следам установки расширения (16.09.2026)

> **Состояние 16.09.** Сделано в сервисе и панели: С37, С38 (`d6b22a2`), С40 (`ff725a1`), П25 (`6b00fde`), П27 (`73be545`). Ждут агента (другой репозиторий):
> А39–А41 и зависящие от них С39, П24, П26. К3 — за администратором сервера 1С.

**Что наблюдали.** Три задания по `_transition`, агент `2026-09-16 09:23`:

| Операция | Итог | Длительность | Текст |
|---|---|---|---|
| Установить расширение | Выполнено | 20 мин 28 с | `_transition: база занята, повтор 16.09.2026 09:30` (устарело, см. П25) |
| Удалить расширение | Выполнено | 4 мин 3 с | — |
| Установить расширение | **Не выполнено** | 3 мин 53 с | «Ошибка разделенного доступа к базе данных. База данных заблокирована: компьютер: SERVER, сеанс: 2, начат 16.09.2026 в 9:54:27, приложение: **Фоновое задание**» |

**Суть.** Базу держит фоновое задание самой 1С. Блокировка входа его не останавливает, снять сеанс панель
предлагает только вручную и в другом разделе, а отказ приходит без кода «занята» — значит, ни подсказки, ни
повтора. Человек видит «Не выполнено» и не знает ни кто держит базу, ни что с этим делать.

---

## Агент

| № | П | Задача |
|---|---|---|
| А39 | В | **«Ошибка разделенного доступа» — это `IB_BUSY`, а не общий отказ.** Контракт уже относит монопольный доступ к `IB_BUSY` (`ONEC_AGENT_CLUSTER_CONTRACT.md:1071`), но этот текст платформы под правило не попал: сервис не повторил команду и не дал подсказку. Сделать: распознавать «Ошибка разделенного доступа», «База данных заблокирована», «exclusive» и отвечать `IB_BUSY` |
| А40 | В | **Держателя базы — отдельными полями `details`.** Текст уже называет компьютер, номер сеанса, время начала и приложение — но только прозой. Дать `details.holder {computer, sessionId, startedAt, appId}` (одна или несколько строк): по ним панель покажет, кто держит, и предложит снять сеанс, не заставляя читать абзац |
| А41 | С | **Команда «запретить регламентные и фоновые задания»** (`rac infobase update --scheduled-jobs-deny=on/off`). Сейчас её нет вовсе. Без неё фоновое задание не остановить: блокировка входа на него не действует, а снятие сеанса помогает до следующего запуска по расписанию |

## Сервис

| № | П | Задача |
|---|---|---|
| С37 | В | **Подсказка к занятости базы фоновым заданием.** Сейчас подсказка есть только для текста «Соединение с информационной базой не установлено» (`onec/errorHints.ts:84`), и на этот отказ она не срабатывает. Сделать: по тексту («разделенн», «разделённ», «База данных заблокирована», «монопольн», «exclusive») — объяснение «базу держит сеанс: <приложение> на <компьютере>; закройте вход, снимите сеанс или запретите регламентные задания на время работ»; отметку «в базу не войти» не ставить **Сделано 16.09 `d6b22a2`.** |
| С38 | В | **Повторять такой отказ в задании.** Сейчас повторяются только коды из `RETRY_LATER_CODES` (`queue.ts:156`). После А39 отказ придёт как `IB_BUSY` и повтор заработает сам; до обновления агента — распознавать по тексту и повторять так же (пауза 120 и 300 с, до трёх попыток) **Сделано 16.09 `d6b22a2`.** |
| С39 | С | **Запрет регламентных заданий в спецификации команд** (после А41): команда кластера, `CLUSTER_SET_SCHEDULED_JOBS {baseKey, deny}`, эхо `state.scheduledJobsDeny` в реестр. Для долгих операций — ставить запрет перед началом и снимать после, как уже делается с блокировкой входа |
| С40 | С | **Время ожидания и время работы — в отчёт задания.** В строках задания нет ни `dispatched_at`, ни `started_at` (`onec/batches.ts`), поэтому «20 мин 28 с» не разделяется на «ждала очереди» и «работала». Отдавать оба поля; панель покажет «ждала N · работала M» **Сделано 16.09 `ff725a1`.** |

## Панель

| № | П | Задача |
|---|---|---|
| П24 | В | **Кто держит базу — в строке отказа, с действием.** По `details.holder` (А40) показать «держит: Фоновое задание, сеанс 2, SERVER» и кнопку «Снять сеанс» / «Закрыть вход»; переход на «Сеансы» с уже выбранной базой. Сейчас человек читает абзац и идёт искать раздел сам |
| П25 | В | **Успешная операция сохраняет устаревшее примечание.** `progress.ts:250-253`: примечание обновляется как «повтор → предупреждение → **прежнее значение**», а строка про повтор считается только пока задание идёт. Поэтому завершённое задание показывает «Выполнено» и рядом «база занята, повтор 09:30» от первой попытки. Сделать: при завершении без ошибок сбрасывать примечание, оставшееся от ожидания повтора **Сделано 16.09 `6b00fde`.** |
| П26 | С | **Долгую очередь объяснять на месте.** Пока команда ждёт места базы, в «Прогрессе» видно только «Выполняется». Показывать «ждёт очереди по базе: перед ней N команд» и, если известен держатель, кто именно её держит (после С40 и А40) |
| П27 | С | **Предупредить до запуска.** В мастере установки расширения на шаге «Что произойдёт» показывать активные сеансы выбранных баз (уже есть `fetchSessions`) — «в базе 2 активных сеанса, среди них фоновое задание: операция может не начаться» **Сделано 16.09 `73be545`.** |

## Сервер 1С

| № | П | Задача |
|---|---|---|
| К3 | С | **Разобраться с фоновым заданием в `_transition`** (сеанс 2 от 16.09 9:54, приложение «Фоновое задание»): что это за задание, нужно ли оно на копии базы. На время работ с расширениями — отключать регламентные задания в консоли кластера (или после А41 — командой панели). Владелец — администратор сервера 1С |

---

**Порядок.** А39 + С37/С38 (отказ становится понятным и повторяется) → П25 (не врать итогом) → А40 + П24 (кто держит
и что нажать) → С40 + П26 (куда ушло время) → А41 + С39 + П27 (запрет заданий и предупреждение заранее).

**Замечание по сборке.** В документах числится агент `2026-09-15 23:24`, а отвечает `2026-09-16 09:23` — обновить
отметку в `CHECKS_SERVER_2026-09-13.md` и `AGENT_LATEST_BUILD` в `ai/.env` (нужен `pm2 restart all`).
