# Аудит согласованности: панель ↔ сервис ↔ агент 1С — задачи от 14.09.2026

Сверка по живым данным (журнал команд, реестр, журнал сервиса) и коду. Сборка агента на
сервере — `0.1.0+2026-09-14 01:35 (+05)`; **14.09 22:49 — окно агента `22:38`, 23:27 — `23:16`; самопроверка
`_transition` пройдена целиком оба раза** (роли всеми способами, `showInList` в обе стороны, запреты;
сессия агента). Документ ведётся до закрытия всех пунктов: при
отходе от плана — сюда же статус и причина.

## Что сверено и сходится

| Что | Итог |
|---|---|
| Команды сервиса ↔ способности агента | все типы и способности описаний у агента есть |
| API панели ↔ маршруты сервиса | сходятся; без вызова из панели — только `GET /bases/:key/info` |
| Эхо пользователей и расширений | применяется: после 9 правок пользователей с 01:35 — ни одного лишнего чтения |
| Отказы команд за сутки | нет |
| Журнал сервиса после перезапуска 14.09 10:19 | предупреждений и ошибок нет (второй экземпляр агента, срез публикаций — 11–13.09) |
| Живые проверки 14.09 | роли (`addRoles`/`removeRoles`/несуществующая) и `showInList` — в обе стороны |

## Задачи

| № | Кто | Задача | Статус |
|---|---|---|---|
| T1 | агент + сервис | Блокировка сеансов из среза баз не доходит до реестра: у 111 баз из 111 состояние пустое, хотя полный срез приходит раз в 5 мин. Сервис: писать в журнал, сколько строк среза пришло с `lock`. Агент: проверить, что фоновое чтение блокировок работает и не стоит на паузе после отказа | **сервис — сделано** (`logLockCoverage`: «полный срез баз: строк с блокировкой сеансов»); агент — **сделано в сборке `2026-09-14 23:16`**: причина — полный срез в heartbeat собирала `collect_bases`, переносившая в `BaseState` только перечисленные поля без `lock`; теперь строка собирается `BaseState::from_cluster_item` и переносит `lock` (тест). На сервере — после установки 23:16 строкой «полный срез баз: строк с блокировкой сеансов» (`TASKS_AUDIT_2026-09-14.md`, А6) |
| T2 | панель | `CLUSTER_DISCONNECT` возвращает `state.locks` (агент R7-А3) — таблица «Блокировки» после разрыва из ответа | **сделано** (`ConnectionsTab`, `echoList(…, "locks")`) |
| T3 | сервис | `AGENT_CANCEL_COMMAND` со снятием процесса возвращает `state.processes` — применять, как у `AGENT_KILL_PROCESS` | **сделано** (`writeState.planWriteState`) |
| T4 | сервис | `IB_APPLY_UPDATE` без эха ставил чтение только расширений; роли могли пропасть (агент R7-А5) — читать и пользователей | **сделано** (`writeState.readsAfter`) |
| T5 | агент → панель | Окно блокировки: при включении «без окна» агент не стирает прежние `from`/`to`; в панели полей окна нет, предупреждение сверяет только `enabled` | агент — **частично, сборка `2026-09-14 23:16`**: в `state.lock` признак `active` (действует ли блокировка сейчас), в ответе `warning`, если включили, а вход не закрыт; загрузка и обновление добиваются действующей блокировки (окно «сейчас — сутки», иначе отказ `SESSIONS_LOCK_NOT_ACTIVE`). **Сборка `23:52`**: при включении незаданное сбрасывается по ступеням (окно — нулевой датой 1С, сообщение и код — пустыми; при отказе `rac` — только окно, затем прежний вызов), в ответе `reset` (`all`/`dates`/`none`) и `note`. Проверить на `_transition`. Панель может показывать `active`, `warning`, `reset` и `note` |
| T6 | панель | Завершённые операции не уходили в историю | **сделано**: без «Истории» «Прогресс» показывает только идущие; число на «Истории» учитывает завершённые |
| T7 | агент (документация) + решение | Контракт `ONEC_AGENT_CLUSTER_CONTRACT.md` не описывает `state.lock`/`infobases`/`config`/`processes`/`publication`/`locks`. Агент объявляет `IB_SELFTEST`, `AGENT_LOG_TAIL`, `AGENT_HEALTH`, `IB_BATCH` — сервис их не знает | контракт — **сделано 14.09** (все блоки эха, раздел «Служебные команды агента», коды `IB_FIELD_NOT_APPLIED`, `SESSIONS_LOCK_NOT_ACTIVE`, `IB_CONNECTION_LOST`); решение принято: `AGENT_HEALTH`, `AGENT_LOG_TAIL`, `IB_SELFTEST` остаются, **`IB_BATCH` агент не объявляет со сборки `23:52`** |
| T8 | проверка на сервере | Вживую прогнать E1–E6: блокировка, удаление мёртвой регистрации, загрузка на `_transition`, обновление на копии, снятие процесса, публикация (эхо только IIS) | ждёт |

## Панель П1–П9 и сервис С7/С8 (из аудита сессии агента, `TASKS_AUDIT_2026-09-14.md`)

Актуальность сверена по коду 14.09; П4 = T2 (уже было сделано).

| № | П | Задача | Статус |
|---|---|---|---|
| П1 | В | Общая форма «изменить пользователя» (`ElementForm`) слала во все отмеченные базы `disabled` всегда и `roles` целиком набором из одной базы | **сделано**: `userUpdate.buildGroupUserUpdate` — только изменённое, роли `addRoles`/`removeRoles` относительно показанного набора; ничего не изменилось — команды нет |
| П2 | В | Загрузка, обновление, проверка базы ждались 15 мин (идут до 4 ч): операция «падала», номер терялся, повтор ставил вторую | **сделано**: ответ «ещё идёт» → операция в «Прогрессе» следит за командой по номеру без предела (`followCommand`), итог — тостом; повтор недоступен, пока работа по базе идёт (`workKey`) |
| С8 | С | Изменяющие команды без `requestId` — повтор ставил вторую загрузку | **сделано**: `commandRequestId` — долгие изменяющие команды по базе (кроме `dryRun`) склеиваются с идущей |
| П3 | С | Согласие на снятие процесса — по тексту; для `AGENT_PROCESS_NOT_FOUND` не предлагалось | **сделано**: по `AiServiceError.code` (`AGENT_PROCESS_UNSAFE`, `AGENT_PROCESS_NOT_FOUND`), текст — запасной путь без кода |
| П4 | С | `state.locks` после разрыва | **сделано** ранее (T2) |
| П9 | С | Переиндексация и пересчёт итогов по умолчанию без «Исправлять» | **сделано**: по умолчанию выключены, доступны и отправляются только с «Исправлять»; итог показывает `skipped` |
| П5 | Н | Прерывание: 202 читалось как «прерывать нечего» | **сделано**: `abortCommand` дожидается ответа агента (до 2 мин) |
| П6 | Н | Проверка баз данных: не видно, какие базы не проверены и почему | **сделано**: «Не проверены: N (база — причина; …)», до 10 баз, итог — предупреждение |
| П7 | Н | Публикации после 202: строки публикаций попадали в список баз | **сделано**: после ожидания реестр перечитывается (`fetchBases`); С7 (сервис отдаёт сырой ответ после 202) панели больше не мешает — оставлено открытым в `TASKS_AUDIT` |
| П8 | Н | `BatchType` объявлял `IB_LIST_USERS`/`IB_LIST_EXTENSIONS`, которых `/batch` не принимает | **сделано**: типы убраны (вызовов не было) |

**Сервис С1–С7, С10–С14 — сделано 14.09** (подробности в строках С `TASKS_AUDIT_2026-09-14.md`;
миграция `ai/migrations/025_command_queue_slots.sql`, действует после `pm2 restart all`):

| № | Что сделано |
|---|---|
| С1 | Место базы и её очередь — только у команд `ib.admin` (`in_base`); снятие сеанса и блокировка входа не ждут загрузку |
| С2 | Срок выполнения — от выдачи агенту; ожидание очереди — отдельно (900 с / 12 ч у задания), причина `COMMAND_QUEUE_TIMEOUT` |
| С3 | Истёкшая без результата команда держит место ещё 10 мин |
| С4 | Проверка с «Исправлять» — только полному доступу |
| С5 | `dryRun` не снимает «в базу не войти»; короткий срок и впереди очереди |
| С6 | `IB_AUTH_FAILED` → «нет доступа» по коду |
| С7 | Публикации после 202 — тот же ответ (реестр + `report`), панель берёт его |
| С10 | `IB_BUSY` в задании — до 3 попыток, отчёт по последней |
| С11 | `canceled` завершает ожидание |
| С12 | Payload расписания — по схеме команды, без секретов |
| С13 | Контракт: сроки, очередь, `IB_CHECK`, коды `TIMEOUT`/`IB_AUTH_FAILED`; подсказка к `TIMEOUT` |
| С14 | `apache22` в публикации |

## Панель и сервис после аудита — 14.09 (вечер)

| № | Что сделано |
|---|---|
| R7-П1 | Карточка базы после создания и удаления пользователя не читает базу вживую — таблица обновляется из реестра по окончании задания |
| R7-П2 | `base-users` (таблица пользователей карточки базы) — в `REFRESH_KEYS` |
| R7-П3 | Общая форма больше не сбрасывает весь кэш `["onec"]` после постановки: задание — операция в «Прогрессе», перечитывание по окончании |
| P2 (`showInList`) | Неизвестное значение — тумблер в среднем положении (`FieldToggle.indeterminate`), а не «выключено» |
| P1 (`showInList`) | Текст отказа уходит в итог операции как есть (`mergeBatch`); вживую — после `pm2 restart all` |
| S1 | Эхо с другим `showInList`, чем записано, — отказ `AGENT_FIELD_NOT_APPLIED` «записано … / в базе …» |
| S2 | Запоминание признака — после применения эха и только если эхо его не принесло |
| S3 | После `IB_FIELD_NOT_APPLIED` ставится `IB_LIST_USERS` |

## Для сессии агента

1. **T1.** Почему в `bases` heartbeat нет поля `lock` (или оно не того вида: нужен объект с
   булевым `enabled`). После перезапуска сервиса число строк с блокировкой видно в его журнале
   строкой «полный срез баз: строк с блокировкой сеансов». Проверить паузу 6 ч после отказа
   фонового чтения и наличие учётной записи, под которой оно идёт.
2. **T5.** Включение блокировки без окна должно стирать прежние `from`/`to` (аргументы `rac`),
   иначе `state.lock` покажет окно прошлой блокировки.
3. **T7.** Описать в контракте блоки эха E1–E6 и `state.locks` у `CLUSTER_DISCONNECT`; по
   `IB_SELFTEST`/`AGENT_LOG_TAIL`/`AGENT_HEALTH`/`IB_BATCH` — либо описать для сервиса, либо не
   объявлять в способностях.
4. **T8** — вместе с администратором, по разделу «Как проверить» в `TASK_AGENT_ECHO_WRITE_COMMANDS.md`.

> **Параллельный аудит сессии агента — `TASKS_AUDIT_2026-09-14.md`** (агент А1–А8, сервис С1–С14,
> панель П1–П9, раздел «Статус»). Общие пункты: T1 = А6, T2 = П4, T3 = С9, T5 = А7, T7 = А8.
> **На 15.09 (сессия агента): пункты 1–3 выше в коде закрыты.** А6 — `23:16` (на сервере); А7
> (сброс незаданного по ступеням, `reset`/`note`) и А8 (`IB_BATCH` не объявляется, контракт) —
> сборка `2026-09-14 23:52`, ещё не установлена; там же А9 (без BOM) и А10 (`IB_CONNECTION_LOST` с
> состоянием кластера). **Агенту осталось только проверить на сервере:** установить 23:52; строка
> «полный срез баз: строк с блокировкой сеансов» > 0 (А6); на `_transition` — сброс окна (А7), А1
> (повтор проверки базы), А2; T8 с администратором. Сервису и панели — С15, П10, П11, П12 (`TASKS_AUDIT`), кратко ниже. **15.09:** С15 и П10 сделаны
сессией сервиса и панели; открыты П11 (данные `cluster.locks` агент отдаёт со сборки `2026-09-15 00:12`)
и П12. На сервере агент `23:52`: А9 и А10 подтверждены (проверка базы `_transition` обрывается
перезапуском рабочего процесса при нулевых правилах кластера — разбор на стороне сервера 1С).
**15.09 00:23:** на сервере агент `00:12`, А11 подтверждён — пределы рабочего сервера тоже 0, процесс
перезапущен: вероятно, `rphost` падает сам. Агент `2026-09-15 00:32` (не установлен): А12 — вывод об этом в
тексте обрыва; «Отладочная информация» с проверкой баз данных. Сервису — С16 (ниже).

## Сервису и панели — после агента 23:52 (сессия агента, 15.09)

| № | Кто | Задача | Где |
|---|---|---|---|
| С15 | сервис | Подсказка к новому коду `IB_CONNECTION_LOST`: «во время операции оборвалась связь с рабочим процессом кластера — процесс упал или перезапущен; текст агента называет процессы, запущенные после начала операции, и пределы памяти кластера. Повторять после разбора причины». В задании этот код автоматически не повторять (в отличие от `IB_BUSY`): тяжёлая проверка или загрузка упадёт так же | `ai/src/onec/errorHints.ts`, правило повтора задания (С10) |
| П10 | панель | После «Закрыть вход» ответ `CLUSTER_SET_SESSIONS_LOCK` несёт `reset` (`all`/`dates`/`none`) и `note`, если прежние окно, сообщение или код не сброшены, — показывать `note` предупреждением (сейчас показываются только `warning` и `active: false`) | `SessionsTab.tsx`, `lock.onSuccess`; тип `SessionsLockResult` в `api.ts` |
| П11 | панель | «Состояние сервера» (`AGENT_HEALTH`): блок `cluster.locks {known, fresh, enabled, paused, lastRefusal}` — «Блокировки: прочитано N из M, включены у K, на паузе после отказа P; последний отказ: база — причина». По нему разбирается А6, если в срезе мало строк с блокировкой | `agentHealth.ts` рядом с `bases` и `publications` |
| П12 | панель + сервис | Ответ `IB_UPDATE_USER`/`IB_CREATE_USER` может нести `unverified: ["showInList"]` (признак не удалось перечитать) и `skipped: [...]` (платформа не приняла необязательные свойства). Сервис — не терять их в результате элемента задания; панель — предупреждение в итоге операции карточки | `onec/batches.ts` (результат элемента), `progress.ts`/`mergeBatch` |
| С16 | сервис | Подсказка к `IB_CONNECTION_LOST` («поднимите пределы в консоли кластера») неверна, когда пределов нет: 15.09 00:23 агент показал все пределы кластера и рабочего сервера = 0, а процесс всё равно перезапущен. Со сборки агента `2026-09-15 00:32` в таком случае текст сам содержит вывод «пределы перезапуска и памяти не заданы … вероятнее всего, аварийно завершился». Совет про пределы — только если этого вывода нет; иначе — «процесс упал: журнал «Приложение» Windows, дампы `rphost`, проверка на копии базы». **15.09 00:42 (агент 00:32):** вывод агента пришёл, а подсказка сервиса рядом по-прежнему «поднимите пределы». Признак вывода во всех его вариантах — фраза «пределы перезапуска и памяти не заданы»; со сборки агента `2026-09-15 01:06` (А13) в тексте могут быть и записи журнала «Приложение» о падении `rphost` | `ai/src/onec/errorHints.ts` |
