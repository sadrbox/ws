# План: новые возможности 1С — что нужно от панели и сервиса

22.09.2026. Парный план к развитию расширения `buhprof_api` (сторона 1С делается отдельно, см. раздел
«Сторона 1С» ниже — он здесь только как контекст). Предыдущие планы:
[PLAN_1C_TASKS_NOTES_2026-09-22.md](PLAN_1C_TASKS_NOTES_2026-09-22.md),
[PLAN_TASKS_NOTES_PANEL_SERVICE_2026-09-22.md](PLAN_TASKS_NOTES_PANEL_SERVICE_2026-09-22.md).

## Состояние на 22.09, вечер: сервис и панель сделаны, живой проверки нет

| Пункт | Что в коде | Чем проверено |
|---|---|---|
| СВ10–СВ11 ✔ | `list_documents` и `list_document_types` в реестре инструментов (`ai/src/tools/registry.ts`), команды `LIST_DOCUMENTS`/`LIST_DOCUMENT_TYPES` | `tests/onec_capabilities.test.ts`: отбор уходит как есть, выдуманный контрагент и «сентябрь» вместо даты не проходят |
| СВ12 ✔ | `create_counterparty` (БИН — 12 цифр), `create_product` (товар/услуга); класс WRITE, карточки подтверждения в `workflow.card()` | БИН из пяти цифр и с буквой отвергнуты до команды; вид, кроме товара и услуги, тоже |
| СВ13 ✔ | `get_debts`, `get_balances`, `get_turnovers`; предел строк ставит сервис (50 по умолчанию, 100 предел) | предел не обойти просьбой модели; кривой БИН организации просто не уходит |
| СВ14 ✔ | `create_cash_order`, `get_cash_order`, `post_cash_order`, `unpost_cash_order`; вид ордера в ссылке берётся из `direction` ответа 1С | сумма ≤ 0 и чужой контрагент отвергнуты; POST/UNPOST — класс CRITICAL |
| СВ15 ✔ | Правила 20–23 в `ai/src/chat/prompt.ts`: за списком — `list_documents`, за суммой — `get_debts`, справочник заводить только после безуспешного поиска и подтверждения | проверено тестом на текст промпта |
| СВ16 ✔ | `POST /v1/onec/bases/:key/self-check` — команда `SELF_CHECK` бизнес-агенту той базы | — (нужен агент: см. зависимость от РБ12 ниже) |
| СВ17 — | Отложен по решению стороны 1С: пока сборка расширения одна, порог защищать нечего | |
| СВ18 ✔ | `ai/tests/onec_capabilities.test.ts` — 18 проверок | `npm test` в `ai`: 382 теста |
| ПН6 ✔ | «Проверка BuhProf в базе» в карточке базы → «Обслуживание»; разбор ответа — `selfCheckView.ts`, мягкий: незнание не красится в отказ | `frontend/src/__tests__/onecSelfCheckView.test.ts` |
| ПН7 ✔ | Колонка «Расширение» в списке баз 1С; пусто — «не видели», а не «устарело» | — |
| ПН8 ✔ | Вкладка «Вызовы чата» в карточке базы + `GET /v1/onec/chat-calls`; вызов склеивается с исходом по `callId` в пределах диалога | тесты склейки в `ai/tests/onec_capabilities.test.ts` |
| ПН9 ✔ | Вкладка «Долги и остатки» в карточке организации + `POST /v1/onec/organizations/:uuid/finance`. **Не кэшируется** — решение владельца 22.09 | `onecSelfCheckView.test.ts`: разбор ответов 1С |

**Чего ещё нет.** Ни одна команда не выполнялась в живой базе. (РБ10 сделан 23.09: расширение загружено в
`Dev_01`, разметка и модули форм платформой разобраны; РБ11 — сквозной тест — дополнен блоком 1.6.0 в тот
же день, но на живой базе ещё не прогонялся. Третий аудит нашёл при этом, что кассовый ордер не
создавался вовсе, см. [AUDIT_CONSISTENCY_3_2026-09-23.md](AUDIT_CONSISTENCY_3_2026-09-23.md).) Белый список агента (**РБ12**) сделан в тот же день — выпуск `2026-09-22 15:10`, все
двенадцать команд на месте, — но на машинах с 1С агент этой сборки ещё не стоит. Пока он не обновлён,
веб-чат ERP, кнопка «Проверка BuhProf» (СВ16/ПН6) и числа организации (ПН9) отвечают `409 CAPABILITY_MISSING`
«обновите агента»: сервис с 22.09 сверяет тип команды с перечнем, объявленным агентом, и не тратит минуту на
ожидание отказа. Чат в форме 1С работает независимо — он исполняет инструменты сам через шлюз.

**Формат payload всех бизнес-команд описан** в [CONTRACT_ONEC_BUSINESS_COMMANDS_2026-09-22.md](CONTRACT_ONEC_BUSINESS_COMMANDS_2026-09-22.md)
(заведён аудитом 22.09: до него контракта не было вовсе — только код реестра инструментов).

**Формат payload новых команд задан сервисом** (см. `buildPayload` в реестре инструментов) и подлежит
сверке со стороной 1С при РБ11: контракта на эти операции в `docs/` нет, а обратной совместимости
соблюдать не перед кем — расширение ещё нигде не установлено.

## Зачем

Сегодня расширение умеет 37 операций (версия 1.6.0): реализации, поступления, счета, акт сверки, банк, печать, отчёты и
поиск по четырём справочникам. Разрыв между этим и тем, что спрашивают у помощника, — в трёх местах:

1. **Списков документов нет.** Документ читается только по идентификатору. «Покажи реализации за сентябрь»,
   «какие счета не оплачены» — ответить нечем.
2. **Справочники только читаются.** Нет контрагента — помощник заходит в тупик: завести его он не может.
3. **Нет чисел.** Отчёт возвращает печатную форму; сравнить, сложить и ответить «кто сколько должен» по ней
   нельзя. Единственная числовая операция — сверка выписки по счёту 1030.

Сторона 1С закрывает это новыми операциями. Сервису и панели нужно их принять, дать модели и показать
человеку. Ниже — только работа сервиса и панели.

## Сторона 1С (контекст, делается отдельно)

Новые операции шлюза и их команды агента:

| Операция | Команда | Что делает |
|---|---|---|
| `documents.list` | `LIST_DOCUMENTS` | список документов вида (реализация, поступление, счёт, ПКО, РКО) с отбором: период, контрагент, организация, признак проведения, сумма |
| `documents.types` | `LIST_DOCUMENT_TYPES` | какие виды документов база умеет отдавать списком (добавлено в 1.6.0) |
| `counterparties.create` | `CREATE_COUNTERPARTY` | завести контрагента по БИН и наименованию |
| `products.create` | `CREATE_PRODUCT` | завести номенклатуру: наименование, единица, вид |
| `analytics.debts` | `GET_DEBTS` | задолженность по контрагентам на дату: кто должен нам, кому должны мы, просрочка |
| `analytics.balances` | `GET_BALANCES` | остатки по счетам учёта на дату (касса, банк, расчёты) |
| `analytics.turnovers` | `GET_TURNOVERS` | обороты за период по счёту с разрезом |
| `cash.create` | `CREATE_CASH_ORDER` | приходный и расходный кассовые ордера |
| `cash.get` | `GET_CASH_ORDER` | прочитать кассовый ордер (добавлено в 1.6.0) |
| `cash.post` / `cash.unpost` | `POST_CASH_ORDER` / `UNPOST_CASH_ORDER` | проведение кассовых ордеров |
| `diagnostics` | `SELF_CHECK` | самопроверка: связь, токен, права, организации с БИН, версия |

Все читающие возвращают **числа и строки**, а не печатные формы: печать остаётся у `documents.print`.

## Сервис (ai)

**СВ10. Новые команды в белом списке.** Добавить перечисленные `commandType` в реестр инструментов
(`ai/src/tools/registry.ts`) и в контракт агента. Команды идут тем же путём, что нынешние: агент — через
очередь, чат в 1С — через `TOOL_CALLS`.

**СВ11. Инструменты модели для списков.** `list_documents` — вид документа, период, контрагент, признак
проведения, предел. Класс `READ`. В описании явно: это список, а не печать; для печати есть `print_document`.

**СВ12. Инструменты создания справочников.** `create_counterparty` (БИН обязателен), `create_product`. Класс
`WRITE` — значит карточка подтверждения. Защита от выдуманных id здесь не работает (объекта ещё нет), поэтому
проверка другая: БИН — 12 цифр, наименование не пустое.

**СВ13. Инструменты аналитики.** `get_debts`, `get_balances`, `get_turnovers`. Класс `READ`. Ответы компактные:
модели нужны суммы и названия, а не проводки. Ограничить число строк (например, 50) и добавить в ответ
`total`, чтобы модель могла сказать «показаны 50 из 320».

**СВ14. Инструменты кассы.** `create_cash_order` (вид: приход/расход), `post_cash_order`, `unpost_cash_order` —
по образцу реализаций, с `requestId` и подтверждением.

**СВ15. Промпт.** Дописать правила: за списком документов — `list_documents`, а не `run_report`; за суммой
долга — `get_debts`; создавать контрагента только когда поиск ничего не нашёл и пользователь подтвердил.
Без этого модель будет тянуться к отчётам, как привыкла.

**СВ16. Самопроверка базы в панели.** Команда `SELF_CHECK` и кнопка в карточке базы: одним нажатием видно,
что не так на стороне 1С (нет токена, нет организаций с БИН, старая версия расширения, нет прав).

**СВ17. Версия расширения как признак возможностей — ОТЛОЖЕН.** Сторона 1С решила иначе (см. «Что осознанно
отложено» в [PLAN_ONEC_EXTENSION_2026-09-22.md](PLAN_ONEC_EXTENSION_2026-09-22.md)): пока сборка расширения
одна, порог защищать нечего, а инструмент, спрятанный по ошибочному порогу, выглядит как пропавшая функция.
Версия по-прежнему приходит в `GET /ping` и в heartbeat агента и показывается в панели (ПН7) — этого хватает,
чтобы понять, чего ждать от базы. Порог вернётся, когда у клиентов окажутся разные сборки.

**СВ18. Тесты.** Новые инструменты: сборка payload, отказ при БИН не 12 цифр, ограничение числа строк,
невидимость инструментов при старой версии расширения.

## Панель (frontend)

**ПН6. Самопроверка базы.** Кнопка «Проверить базу» в карточке базы 1С и понятный разбор ответа `SELF_CHECK`:
список проверок с отметками и подсказкой, что сделать. Сейчас такие вещи выясняются по частям.

**ПН7. Версия расширения на виду.** В списке баз — колонка с версией расширения и отметкой «устарело», если
ниже порога из СВ17. Без неё «почему у этой базы нет списков документов» выясняется вручную.

**ПН8. Журнал вызовов чата.** Отдельная вкладка: какие инструменты вызывались, с какой базой, чем кончилось
(успех, отказ 1С, отказ ERP). Сейчас события чата видны только в журнале сервиса, а события агента — в панели;
для разбора жалоб этого мало (дополняет СВ8).

**ПН9. Карточка организации: числа из 1С.** Когда появятся `get_debts` и `get_balances`, показать в карточке
организации задолженность и остатки, обновляемые по кнопке. Это первый случай, когда панель показывает данные
не из ERP, а из базы 1С — решить, кэшировать ли их и на какой срок.

## Порядок

Выполнено в один заход 22.09: СВ10–СВ16, СВ18, ПН6–ПН9 (см. таблицу состояния вверху). СВ17 снят как
отложенный. Осталась не работа сервиса и панели, а проверка в живой базе:

1. **РБ10** (сторона 1С) — загрузить расширение в `Dev_01`.
2. **РБ11** — прогнать операции целиком: список документов, заведение справочников, долги и остатки, касса,
   самопроверка. Здесь и сверяется формат payload, заданный сервисом.
3. **РБ12** — новые команды в белом списке `bpapi_agent`. До этого веб-чат ERP, «Проверка BuhProf» и числа
   организации отвечают `UNKNOWN_COMMAND`.
4. Затем — проверка на живой паре и правка формата, если сторона 1С назовёт поля иначе.

## Решённое

- **Числа из 1С в панели НЕ кэшируются** (решение владельца, 22.09). Кэш означал бы третью версию правды —
  1С, кэш, панель — и вместо ответа начинался бы разговор «почему суммы разные». Цена принята: при
  недоступном агенте карточка говорит, что чисел нет, вместо вчерашних, выданных за сегодняшние. В ответе
  есть `readAt` — на какой миг числа верны.
- **Порог версии расширения** (СВ17) — отложен, см. выше.
