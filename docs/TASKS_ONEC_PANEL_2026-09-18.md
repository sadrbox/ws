# Панель 1С: работа 18.09.2026 и что осталось

## Сделано

| Что | Коммит | Суть |
|---|---|---|
| Выборочная проверка баз данных в «Обновить» | `e2d6981` | Реестр помнит, когда базу проверяли (миграция **032**). «Обновить» проверяет только новых и тех, кого не проверяли сутки, не больше 20 за раз; молчит, пока всё на месте. Полная проверка осталась в «Операциях» |
| Номер строки — из её идентификатора | `296a1fb` | «Базы», «Сеансы», «Соединения», «Процессы» нумеровали строки по порядку: строка исчезала — отметки и активная строка переезжали на соседа. Номер считается из ключа строки (`utils/stableRowId`, `backend/utils/onecBasesList`) |
| Тесты маршрутов сервиса | `11c8d1d` | Стенд на живом express с подставными зависимостями (`ai/tests/onec_routes.test.ts`): порядок «список баз → публикации», выборочная проверка, удаление записи о базе, 404 по неизвестной базе |
| Проверка заголовков колонок | `11c8d1d` | `check_translations.js` не запускался с апреля (CommonJS в ESM-пакете) → `frontend/scripts/check-columns-i18.mjs`, с `--strict` для CI |

Ранее в этот заход (17.09): отметки строк при поиске и отборе, «Операции» по отмеченным базам с разделами,
снятие регистрации и удаление записи о базе, колонка «Регламентные задания», «Обновить» с публикациями,
уникальные идентификаторы технических сообщений.

## Найдено попутно, не чинил

**15 колонок без перевода заголовка** (`node scripts/check-columns-i18.mjs`): `Deals` (4), `ImportDeclarations` (3),
`FiscalReceipts` (2), `MonthCloses` (2), `OrganizationAccountingSettings`, `FixedAssetAcceptances`, `TodoStatuses`,
`Products`. Панели 1С не касается — чужие разделы, и слова там подбирать их владельцу.

## Осталось по панели

| № | П | Задача |
|---|---|---|
| П34 | С | **Порядковые номера строк — ещё в семи списках** (`BatchesTab`, `AgentsTab`, `ExtensionsTab`, `UsersTab`, `ServerTab`, `GroupCommandWizard`, `ElementForm`): там строки исчезают реже, но правило то же — `withStableIds` |
| П35 | С | **Подтверждение групповой команды показывает только число баз.** Для разрушающих («Снять регистрацию», «Снять публикацию») стоит показать список ключей (первые 10) — человек видит, по чему именно пойдёт команда |
| П36 | Н | **`dbCheckedAt` нигде не показан.** Колонка или строка в карточке «Базу проверяли: …» — чтобы было видно, насколько свежа отметка «нет в СУБД» |

## Ждёт сервера 1С

`К1` — `IB_CHECK` роняет `rphost` на `_transition`; `К2` — расширение собрано под другую конфигурацию;
`К3` — фоновое задание в `_transition`; `К4` — дубль `new2` в справочнике «Пользователи».

## Эксплуатация

**Нужен `pm2 restart all`.** С прошлого перезапуска (17.09 22:06) в сервисе накопились: миграции **030–032**,
`/my-work`, запрет регламентных заданий, `DELETE /bases/:key`, групповые задания `IB_INFO`,
`CLUSTER_SET_SCHEDULED_JOBS`, `CLUSTER_DROP_INFOBASE`, «Обновить» с публикациями и выборочной проверкой баз;
в бэкенде — `scheduledJobsDenied`, сортировка «Статуса» и «Регламентных заданий», отбор скрытых, стабильные
номера строк.
