-- Контрагент может быть без БИН: физлицо, розничный покупатель, а также элементы,
-- приходящие из 1С (там БИН часто не заполнен). Организация — наоборот, обязана иметь
-- БИН: по нему при приёме событий 1С определяется, к какой организации они относятся.
--
-- @unique остаётся: в Postgres он допускает несколько NULL, но не даст завести двух
-- контрагентов с одним БИН — по нему идёт сопоставление при обмене.
ALTER TABLE "counterparties" ALTER COLUMN "bin" DROP NOT NULL;
