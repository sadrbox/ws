-- Синоним расширения (E15).
--
-- ЗАЧЕМ. В 1С у расширения два имени: Имя (служебное, латиницей — «shahs», «EF_00_00062442»)
-- и Синоним (то, как его называет человек). В реестре мы хранили только первое, поэтому
-- сводка по расширениям читалась как список технических идентификаторов, а понять, что
-- это за расширение, было нельзя. Группировка в панели идёт по паре имя+синоним.
ALTER TABLE base_extensions ADD COLUMN synonym text;
