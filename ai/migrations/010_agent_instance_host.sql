-- Откуда пришёл экземпляр агента (E15).
--
-- ЗАЧЕМ. Счёт экземпляров сказал «их два», но не сказал главного — что они на РАЗНЫХ
-- МАШИНАХ. Именно так и было: один токен прописали и на сервере 1С, и на машине
-- разработки, оба процесса разбирали одну очередь, и команды отказывали через раз.
-- Адрес источника превращает «экземпляров: 2» в «работают с двух машин: A и B»,
-- то есть в готовый ответ вместо новой загадки.
ALTER TABLE agent_instances ADD COLUMN remote_addr text;
