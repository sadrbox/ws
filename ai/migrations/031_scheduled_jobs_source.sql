-- Откуда известен запрет регламентных заданий (С40): `cluster` — прочитано у кластера после записи, `command` — записано
-- по команде панели, когда кластер состояние не отдал. У «по команде» нет времени чтения: панель не должна показывать
-- «запрещены · 23:45», будто это прочитано.
ALTER TABLE bases ADD COLUMN scheduled_jobs_source text;
