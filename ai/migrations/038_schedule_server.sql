-- Сервер 1С у расписания обслуживания (C9/C10, 20.09).
--
-- Имя базы уникально только в пределах сервера: при нескольких серверах расписание по одноимённым базам не знало,
-- чьи базы обслуживать, и пропускало их. NULL — сервер не назван: так работают установки с одним сервером, и
-- поведение прежнее.
ALTER TABLE maintenance_schedules ADD COLUMN IF NOT EXISTS server_id uuid REFERENCES servers(id) ON DELETE SET NULL;
