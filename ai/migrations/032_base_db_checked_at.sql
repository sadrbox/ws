-- Когда у базы в последний раз проверяли НАЛИЧИЕ БАЗЫ ДАННЫХ в СУБД (CLUSTER_CHECK_BASES).
--
-- ЗАЧЕМ. «Обновить» в списке баз перечитывает кластер за доли секунды, а полная проверка баз данных по всем базам
-- идёт десятки секунд (живой замер 17.09: 111 баз — 34 с) и стучится в СУБД по каждой базе. Встраивать её целиком
-- в «Обновить» нельзя, а не проверять — значит узнавать о пропавшей базе от человека. Отметка позволяет проверять
-- ВЫБОРОЧНО: базы, которых ещё не проверяли (только что появились в кластере), и те, что давно не проверялись.
--
-- Отметку ставит ответ агента: проверена та база, про которую он сказал «есть» или «нет» (dbMissing — булево).
ALTER TABLE bases ADD COLUMN IF NOT EXISTS db_checked_at timestamptz;
