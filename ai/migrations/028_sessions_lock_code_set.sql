-- Задан ли код разрешения у блокировки начала сеансов (аудит 15.09, С26, П17).
--
-- Агент кода не отдаёт никогда — только признак `permissionCodeSet`. Без него панель не могла сказать,
-- что в закрытую базу можно войти по коду, и признак отбрасывался при разборе. NULL — не сообщал.
ALTER TABLE bases ADD COLUMN sessions_denied_code_set boolean;
